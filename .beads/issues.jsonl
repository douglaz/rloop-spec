{"id":"rl-58o","title":"Rewrite gate: rloop rebuilds itself from the spec (F1) — owner-picked only, not ready","description":"**Owner-picked only. Not ready: do not select this bead in a Sequence or an uninstructed Run.**\nIt stays open so `F1`'s idea is not lost, at P4, and an agent that reads it cold cannot finish\nit: it needs a released Implementation binary, a seed repository this repository does not hold,\nlive CLI quota, and half of the gate living in rloop-bash.\n\n`07-open-findings.md` F1: the previous release drives `--auto` on a seed repository holding only\nthe pinned spec; pass = `conformance/run` green and one real Run completes; every run of the gate\nrecorded. `Deferred by the owner as an interesting idea for later; the shape sketched here is the\none to start from` (deferred 2026-09-17).\n\n## Before this can be worked\n\nThe owner supplies: the previous-release executable and its revision, the Specification pin, the\nseed repository, the live-run budget, the gate invocation and where its evidence is kept. Until\nthen there is nothing to brief.\n\n## Not in scope\n\nAnything that requires a released Implementation binary, a live CLI Run, or a change in\nrloop-bash. When activated, this bead is the design of the gate written into `F1` and nothing\nelse; the executable halves are filed separately, one per repository.\n\n## Acceptance (when activated)\n\n`F1` states the gate's inputs, its pass condition, where each half lives, and what records it\nkeeps. `nix develop --command bash tools/check-all.sh` exits 0, unpiped.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-18T01:25:32.768587928Z","created_by":"master","updated_at":"2026-09-23T16:36:04.089956948Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"labels":["deferred"]}
{"id":"rl-sna","title":"Live check CNF-22 against the first Implementation","description":"Once rloop-bash exists: a lone Run and a two-Run Sequence against the real CLIs at the versions AGT-17 names; keep the Run Directories and the commits as the record.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-18T01:25:32.816247877Z","created_by":"master","updated_at":"2026-09-19T03:38:44.826566166Z","closed_at":"2026-09-19T03:38:44.826517925Z","close_reason":"done","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"comments":[{"id":1,"issue_id":"rl-sna","author":"master","text":"Done 2026-09-19 against rloop-bash e8b215d, spec 7a463bb. Lone Run: STATUS done, exit 0, 1 Round, commit b8fdf0b. Sequence: Run 1 done (a61e3f7), Run 2 idle, exit 0. Record: 07-open-findings.md F6.","created_at":"2026-09-19T03:38:44Z"}]}
{"id":"rl-panel-membership-in-trace-lhx","title":"Panel membership visible in the scenario trace (#3 prefactor)","description":"Make the Panel's membership visible in the scenario trace, so the Conformance Suite can see a\nReviewer that was never spawned. Pure prefactor for #3: no requirement changes, the suite stays\ngreen, and the enumeration does not grow.\n\n## Why\n\n`observed_trace()` in `conformance/run` (lines 46–68) reconstructs `panel<r>:<class>` by counting\nonly the Reviewers that recorded a run, so three-spawned-three-ok renders as `panel<r>:all`,\nbyte-identical to four. The formal side has the same hole: `tools/formal/Rloop/Loop.lean:95`\nmakes the Panel a single `Spawn` constructor carrying a `Panel` class, and `Scenarios.lean:74`\nrenders it `s!\"panel{r}:{panelName p}\"` — there is nowhere to record *which* Reviewers ran.\n\n**All 206 committed scenarios would stay green over an Implementation that silently ran three\nReviewers.** Same class as the CodeRabbit finding on #2.\n\nThis contradicts #3's settled comment, which said the Lean model needs no change. That is true of\nthe decision function and wrong about the trace, where the Suite's coverage actually lives — see\nthe correction comment on #3 (2026-09-20T21:28:04Z). `tools/formal/` is in scope.\n\n## What to change\n\n- `Spawn.panel` carries the membership.\n- The render in `Scenarios.lean` emits it.\n- `python3 tools/check_scenarios.py --write` rewrites all 206 rows with the longer trace token;\n  commit what it writes (`AGENTS.md`: the scenarios file is rendered, not hand-kept).\n- `observed_trace()` in `conformance/run` is taught to match.\n\n## Why it does not grow the enumeration\n\nUnder today's requirements every Panel is the same four Reviewers, so membership is a constant.\nThe alphabet is unchanged; only the token is longer. Availability as an *axis* is a different\njob and belongs with the requirement change — see the availability-scenario-axis ticket.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all six gates PASS.\n- `conformance/scenarios.tsv` still has 206 rows, each with membership in the trace column.\n- A fake scripted to skip one Reviewer **fails** a scenario that expects four. It does not today;\n  that failure is the whole point of this ticket and must be demonstrated, not asserted.\n- No requirement text changes. `git diff` touches only `tools/formal/`, `tools/`-written files\n  and `conformance/`.\n\n## Lands next to\n\n`F4` (\"The scenario alphabet is a choice\"), whose closing line now has a sibling case: an\nImplementation that passes the suite while running a three-Reviewer Panel.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T02:06:15.769553296Z","created_by":"master","updated_at":"2026-09-21T17:25:52.975254013Z","closed_at":"2026-09-21T17:25:52.975205581Z","close_reason":"Accepted after Round 2: Panel membership is rendered and observed, timed-out Reviewers remain members, 206 scenarios preserved, omission controls S2/S38 reject skipped sol, all six gates and executable conformance with self-check pass.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-availability-probe-jy4","title":"Specify the model-availability probe and the fail-open rule (#3)","description":"Specify the model-availability probe: its command lines, when it runs, and the fail-open rule.\nThe first half of #3. Nothing changes about what rloop does with the answer — that is the\nrecord-omitted-reviewer ticket.\n\n## Why\n\nAn exhausted model does not fail fast. It hangs, writes zero bytes on both streams, and is killed\nat its seat's timeout. A Reviewer seat pays `--reviewer-timeout` (1800s default) **every Round**\nfor a model that was never going to answer.\n\nVerified 2026-09-20: `claude -p \"/usage\"` exits 0 in seconds and reports per-family limits with\nreset times, and answers just as fast *through* the exhausted model, so one call covers every\nclaude family. codex exposes no quota at all; both codex seats share one ChatGPT account, so a\ntiny `codex exec` call is the only direct probe and one probe covers both.\n\n## Settled already (#3, do not re-derive)\n\n- **The check runs immediately before the Panel, never at Run start.** `astra` and `sol` produced\n  the same counterexample independently: an Implementer call may run for hours past a quota reset,\n  so evidence gathered at Run start can suppress a Reviewer that would have answered.\n- **Fail open.** Unknown availability, a probe that fails, output that does not parse, or an\n  unrecognised model name MUST spawn the Reviewer normally. Only positively recognised, unexpired\n  exhaustion may short-circuit.\n- **Remove only, never substitute.** Substitution silently changes which model judged the work.\n- **The parser lives in this Specification.** The \"language-neutral\" objection is answered in #3\n  point 7: this set is language-neutral about the *Implementation* (`ADR-0001`) and is\n  deliberately harness-specific everywhere else — it already hardcodes the model names, the\n  `--effort` values and the deny list byte for byte, and `AGT-17` pins the CLI versions.\n\n## What to change\n\n- A new requirement for the probe's command lines, stated verbatim like every other\n  (`ADR-0001`, `OVR-2`), with `tools/check_fixtures.py --write` regenerating the fixtures. Never\n  edit `conformance/fixtures/` by hand.\n- **Narrow `OVR-1`**, which forbids the probe as written: *\"It MUST NOT parse a Reviewer's\n  output, an Implementer's output, or any part of the Finished File beyond its first line.\"* A\n  quota probe is rloop parsing a model CLI's output and acting on it. Follow `F9`'s six-day-old\n  precedent exactly: it narrowed `DIR-4`'s \"entire and *unparsed*\" to \"entire and *unmodified*\"\n  to admit one datum. This is the second instance of that pattern.\n- `AGT-17`: record the CLI versions the `/usage` format was verified against. `AGT-17` already\n  concedes this risk class — *\"a flag that a newer CLI rejects is found only by a live Run\"* — so\n  a drifted format is not a new kind of exposure, and fail-open degrades it to doing nothing.\n- `CNF` items in `06-conformance.md` for the three fail-open paths: probe fails, probe output\n  does not parse, unrecognised model name. Distinguish exhaustion from a typo: verified,\n  `claude -p \"say hi\" --model claude-bogus-9` exits 1 in seconds with\n  `[claude-code:unrecognized_model]`, while exhaustion hangs with zero bytes.\n\n## ADR\n\n**`ADR-0006`**, not `ADR-0005`. #3's settled comment reserved 0005, and 0005 was taken on\n2026-09-21 by \"The operator's view has one home and Implementations point at it\". Check\n`docs/adr/` for the next free number before writing, not this ticket.\n\nSubject: rloop reads a vendor's human-facing quota report and fails open. Hard to reverse,\nsurprising without context, and argued against by all three advisers — record what they said and\nwhy it was overridden.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all six gates PASS.\n- `grep` finds the probe's command line in exactly one document, and its fixture is generated.\n- Every new requirement is cited by a `CNF` item. The coverage gate holds at zero with no ratchet\n  baseline, so an excuse-table entry is not available here — and would be false, since #3's\n  Testability section says all of this is black-box testable.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T02:06:15.817051407Z","created_by":"master","updated_at":"2026-09-21T20:01:56.760136217Z","closed_at":"2026-09-21T20:01:56.760084860Z","close_reason":"AGT-18, RUN-21, ADR-0006; OVR-1 narrowed per F9, OVR-2/RUN-7/AGT-1/AGT-13 amended, DIR-4 rows, CNF-24-26 with implementations and a probe role in the fake. Six gates exit 0. Suite is 20/4 against rloop-bash: CNF-9/24/25/26 red until it implements the probe, which is ADR-0001's designed path.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-record-omitted-reviewer-hhb","title":"Record an unavailable Reviewer in place, never drop it (#3)","description":"An unavailable Reviewer stays in the Panel and is recorded as not having run, rather than being\ndropped so the Panel runs with three. The second half of #3, and the part that changes behaviour.\n\nDepends on the availability-probe ticket: fail-open means unknown availability MUST spawn\nnormally, so without a probe command line this arm is unreachable in a live Run and unscriptable\nin the Suite — there is nothing for a fake to answer.\n\n## Settled already (#3, do not re-derive)\n\n- **Record, do not drop.** Consequence: there is no Panel floor to choose. `RUN-15` already fixes\n  one — one survivor is enough, zero is exit 2 — and no version of this change alters it.\n  Question 1 of #3 is withdrawn.\n- **\"Did not run\" counts toward `RUN-15`'s zero-survivor abort.** An account wholly out of quota\n  therefore loses the Round: the Implementer may have run for hours and committed work, and rloop\n  exits 2 with no judge and no Finished File. Accepted unchanged — four timeouts reach the\n  identical exit today, two hours later, and `F10` already rejected having rloop author a\n  `finished.md` of its own for precisely this case.\n- **A distinct literal is required.** `REVIEWER FAILED (exit <status>)` cannot describe a call\n  that never happened — there is no status — and `PRM-2` tells the Manager that line means\n  `crashed or timed out`. Reusing it makes both passages false and costs the Manager the\n  distinction `RUN-15`'s own rationale rests on: *\"One Reviewer down is a degraded Panel the\n  Manager can weigh; four down is a broken environment.\"*\n- **No new noun.** A `Seat`/occupant split was proposed and withdrawn by its own proposer.\n\n## What to change\n\n- `RUN-15`: a third arm for the omitted call, and which failures count toward the abort.\n- `RUN-7` §3 — *\"all four Reviewer calls … started concurrently and all waited for\"* — goes\n  false. Amend it.\n- `RUN-8`.\n- `PRM-2`: the distinct literal. Amended either way, per the settled comment.\n- `DIR-4`: the Feedback File's authorship when rloop writes it in the Reviewer's place, and an\n  absent or empty `.err`.\n- `CONTEXT.md`: the two glossary entries drafted in #3 land **here**, not earlier — the Feedback\n  File entry cites a `RUN-15` clause that does not exist until this ticket.\n\n  ```\n  **Panel**:\n  The **Reviewers** `AGT-11` designates for one **Round**, each a different model, run side by\n  side.\n\n  **Feedback File**:\n  One **Reviewer**'s findings for one **Round**, written for the **Manager**; rloop writes it in\n  the Reviewer's place when the call failed or was never made (`RUN-15`).\n  _Avoid_: Review, findings file\n  ```\n\n- `PRM-4` does **not** change. `PRM-6` forbids conditional text, and \"one of four independent\n  Reviewers\" stays true of a designated roster.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all six gates PASS.\n- All four unavailable exits 2 with no judge spawned and no Finished File — **instantly**, not\n  after four timeouts. This is `RUN-15`'s existing rule firing on the new arm, not separate\n  behaviour, which is why it has no ticket of its own.\n- The Manager can tell \"did not run\" from \"crashed or timed out\" by reading the Feedback File\n  alone. Grep `PRM-2` for both literals.\n- Before writing that a clause landed, `grep` for its identifier. A decision gets its identifier\n  when it is accepted (`AGENTS.md`).\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T02:06:15.854188039Z","created_by":"master","updated_at":"2026-09-21T20:40:13.602200463Z","closed_at":"2026-09-21T20:40:13.602150629Z","close_reason":"RUN-15 third arm + zero-survivor counting both kinds of down; RUN-7/RUN-8/DIR-4/PRM-2/CONTEXT.md amended; CNF-27 added incl. the mixed abort case. Six gates exit 0; suite 18/7 against rloop-bash (CNF-11/12 join the reds because PRM-2 changed). F13 records that the Lean pair proves the failure arm only. 'All four unavailable' is unreachable - no codex probe - and RUN-15 is left unscoped because mixed down states are reachable.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-record-omitted-reviewer-hhb","depends_on_id":"rl-availability-probe-jy4","type":"blocks","created_at":"2026-09-21T02:06:17.570569027Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-availability-scenario-axis-rnp","title":"Availability as a scenario axis: the six cases (#3)","description":"Add availability as an axis in the scenario alphabet and generate the six cases #3 requires\nbefore any of this is trusted. This is the ticket that grows the enumeration.\n\nDepends on the panel-membership-in-trace ticket (the trace needs somewhere to put membership) and\non the record-omitted-reviewer ticket (the behaviour has to exist to be enumerated).\n\n## Why separate from the prefactor\n\nMaking membership visible does not grow the alphabet: under today's requirements every Panel is\nthe same four Reviewers, so membership is a constant. Enumerating *availability* multiplies the\ngenerated set, so it belongs with the requirement change rather than with the mechanical\nrewrite. Splitting them keeps a 206-row regeneration reviewable on its own.\n\n## The six cases, from #3\n\n1. One Reviewer unavailable — recorded, not spawned; the Run continues with the rest.\n2. All four unavailable — exit 2, no judge spawned, no Finished File.\n3. A Reviewer re-admitted in a later Round, once the fake reports capacity again.\n4. A probe that fails — fail open, the Reviewer is spawned.\n5. A probe whose output does not parse — fail open, the Reviewer is spawned.\n6. An unrecognised model name — fail open and loud, not a silently smaller Panel.\n\nCases 4–6 are the fail-open paths. They are the ones that make the rule falsifiable; without\nthem \"fail open\" is a sentence nobody can check.\n\n## What to change\n\n- Availability as an axis in `tools/formal/Rloop/Scenarios.lean`'s alphabet.\n- `python3 tools/check_scenarios.py --write`, then commit what it writes.\n- The fakes in `conformance/` gain a way to report exhaustion and to report capacity again.\n- `conformance/scenarios.tsv`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all six gates PASS.\n- Each of the six cases fails against an Implementation that gets it wrong. Demonstrate the\n  failure for each; a scenario that passes both ways tests nothing. This is the lesson `F5`\n  already paid for — *\"a check and its subject built from one misreading agree with each other\"*.\n- The row count grows, and the growth is reported in the commit message with the before and\n  after.\n\n## Lands next to\n\n`F4` (\"The scenario alphabet is a choice\"). If this materially multiplies the set, say so in\n`F4` — it is the finding that owns the size of the enumeration.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T02:06:15.891210896Z","created_by":"master","updated_at":"2026-09-22T22:28:58.549906003Z","closed_at":"2026-09-22T22:28:58.549858242Z","close_reason":"Accepted after Round 1. Behaviour gained 'available'; a Panel's spawn names the Reviewers called; the zero-survivor abort counts not-run as down under a guard of its own, notRunDown, with three theorems and 3 control lines. 206 -> 227 scenarios, the base rows byte-identical once the probe column is dropped, the six cases appended as a block like interference. Case 2 is the reachable mixed form (RUN-21 gives no codex model a family). All eight mutants of rloop-bash go red after the same lines pass unmutated. Verified by the Manager: check-all.sh exit 0 with 7/7 PASS, conformance/run --self-check 25/0, test-panel-trace exit 0. F13 closed, F4 records the growth. Follow-ups: rl-adr-0006-untouched-model-stale-tas, rl-probe-shape-fallthrough-2dc, rl-probe-block-some-class-1fx.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-availability-scenario-axis-rnp","depends_on_id":"rl-panel-membership-in-trace-lhx","type":"blocks","created_at":"2026-09-21T02:06:17.629116617Z","created_by":"master","metadata":"{}","thread_id":""},{"issue_id":"rl-availability-scenario-axis-rnp","depends_on_id":"rl-record-omitted-reviewer-hhb","type":"blocks","created_at":"2026-09-21T02:06:17.688065897Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-prm4-shortcut-swallows-item-0-nyl","title":"PRM-4's clean-path shortcut swallows item 0 (#4)","description":"`PRM-4`'s report block contradicts itself on the clean path, and the contradiction is why a codex\nReviewer's bare `No findings.` carries no provenance. Fix the prompt; no vendor reasoning needed.\n\n## The defect\n\nItem 0 says *\"Say 'I ran everything I needed' if that is true\"*. The block then ends:\n\n```\nIf there is nothing to report, write exactly: No findings.\n```\n\n`write exactly` names a literal, and it is the last and most specific instruction in the block. A\nReviewer with nothing to report is being told to emit that string and nothing else. The codex\nReviewers comply; `opus` is the one deviating by adding item 0 on top.\n\nEvidence is on rloop-spec#4 (2026-09-21 comment): across seven Run Directories every bare report\nis **exactly 13 bytes**, and every codex Reviewer that was itself blocked wrote item 0 in full —\nincluding `sol`, which #4's original text reports as never complying.\n\n## The change\n\nIn `03-prompts.md`, `PRM-4`, replace the closing line of the report block with:\n\n```\nIf you have nothing for items 1 and 2, write item 0 and then exactly: No findings.\n```\n\nKeeps the literal that makes a clean report machine-recognisable; stops it swallowing item 0.\n\nThen `python3 tools/check_fixtures.py --write`. Never edit `conformance/fixtures/` by hand\n(`AGENTS.md`). A prompt is changed in `03-prompts.md` and nowhere else.\n\n## What follows in the Implementation\n\n`PRM-4`'s bytes are normative (`OVR-2`), so `rloop-bash` changes its hardcoded prompt and bumps\nits `spec/` pin, then `spec/conformance/run ./result/bin/rloop` must pass. That is `ADR-0001`'s\ndesigned path, not an accident of it. `CNF-11`/`CNF-12` compare the hardcoded prompt to the\nfixtures byte for byte, so a missed byte fails the suite rather than going live.\n\n## What does not change\n\n`conformance/run` lines 164 and 320 compare a Feedback File to `\"No findings.\"` exactly, but those\nare **fake** Reviewers' canned outputs testing rloop's pass-through plumbing. A fake does not read\na prompt. Leave them.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all six gates PASS.\n- `grep -n \"No findings\" 03-prompts.md` shows the literal in exactly one place, inside the new\n  sentence.\n- The regenerated fixture for `PRM-4` matches the document; `check_fixtures.py` proves it.\n- In `rloop-bash`: `spec/conformance/run ./result/bin/rloop` passes against the bumped pin.\n\n## Verification is a live observation, not a suite item\n\nOnly a live Run shows whether the reworded block is followed — the Suite proves the prompt was\n*sent*, never that a model obeyed it (`00-overview.md`, *How much to trust this*). Record it as a\n`CNF-22` observation with the next release, against a specific prediction: **a clean codex report\ncarries item 0 and the literal both.** If it still emits 13 bytes, the cause was not the shortcut\nand #4's option 2 — telling the Manager in `PRM-2` to weigh a report with no item 0 — is next.\n","status":"closed","priority":2,"issue_type":"bug","created_at":"2026-09-21T02:07:36.458566877Z","created_by":"master","updated_at":"2026-09-22T02:09:56.750900526Z","closed_at":"2026-09-22T02:09:56.750848028Z","close_reason":"Accepted exact PRM-4 amendment and regenerated fixture in Round 1; Manager verified byte preservation and all specification gates PASS. Remaining Implementation adoption tracked in rl-bvz; live observation tracked in rl-vgy.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-restatement-and-citation-gates-rzt","title":"Restatement check plus the ported citation gate (F2, #11)","description":"Catch a rule restated outside its home. `F2`'s trigger fired: one pull request produced eight\nfindings of one rule living in more than one place, in a set of 102 requirements that six gates\ncall green. Decided 2026-09-21 on rloop-spec#11; `F2` records the decision.\n\n## Two parts, and they compose\n\n**1. The restatement check, in `tools/check_ids.py`.** That file already parses every requirement\ndefinition (`DEF_RE`) and every citation (`CITE_RE`) across the root documents and the ADRs, so\nthis is an addition to an existing pass, not a new gate:\n\n> A line that cites another requirement's identifier while carrying an RFC-2119 keyword of its own\n> must either be that requirement's own definition, or carry the owner's words verbatim in\n> backticks.\n\nThe verbatim-quote exemption is not a new concession — `AGENTS.md` already demands it: *\"**Quote\nthe sentence.** A claim about what another requirement says carries that requirement's own words\nin backticks.\"*\n\n**Probed 2026-09-21: 8 lines in the whole set.** Reviewable in one sitting. Two of them\n(`01-run-lifecycle.md:221`, `03-prompts.md:112`) already quote and clear themselves. At least one\nis a real second normative copy: `01-run-lifecycle.md:23` — *\"A lone Run MAY start on a dirty\ntree; a Run inside a Sequence MUST NOT (`SEQ-4`)\"* — states `SEQ-4`'s rule itself while citing it.\nTriage all eight; each is either the home, a quote, or a defect.\n\n**2. Port `check_citations.py` from provisiond-spec.** It is at\n`~/projects/provisiond-spec/tools/check_citations.py`, 284 lines, with a `citation-baseline.json`\nalongside it. It flags a quoted phrase attributed to a requirement whose body does not contain it\n— which is exactly what makes part 1's escape hatch real rather than honour-system. Without it a\nwriter can claim to quote and be wrong, which is how drift instances 1 and 4 survived\n`check_fixtures.py`: the fixture faithfully reproduced the wrong sentence.\n\nWire both into `tools/check-all.sh`, each exit code captured directly, never through a pipe.\n\n## Deliberately not done\n\n`check_obligations.py` stays deferred, recorded in `F2`. Of the eight instances the one obligation\nfinding was a paraphrase, not a duty assigned to another requirement's subject. Build it when an\ninstance of that shape appears.\n\n## Two of #11's four shapes need nothing\n\nRecorded so they are not rebuilt:\n\n- **In an Implementation repository.** `rloop-bash`'s copy was deleted for a pointer\n  (`ADR-0005`, douglaz/rloop-bash#7). Nothing left to drift, and no check in this repository could\n  reach it anyway.\n- **In non-normative prose.** `CONTEXT.md`'s example dialogue now uses no value this Specification\n  owns — no identifier, no filename, no exit status. A survey of every example dialogue in the\n  author's repositories found nine, of which rloop's was the only one naming a spec-owned filename\n  and the only one restating identified requirements; the other eight already followed the\n  convention with no rule and no gate. There is nothing systemic here to enforce.\n\nWhat remains is shape 1, across files — which is the shape a machine can find, because both ends\nname an identifier. Shape 2, a requirement contradicting itself internally, has no mechanical\ncatch and stays a review duty.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with every gate in\n  `README.md`'s *Gates* table passing — the existing identifiers gate now carrying the\n  restatement check, and the ported citation gate added beside it. *This read \"eight gates\"\n  until 2026-09-22 and was wrong twice over: six plus one is seven, and a count written into\n  prose is what `AGENTS.md`'s \"Cite the list; let it hold the number\" forbids. A Manager\n  blocked the Run on the contradiction rather than choosing a reading (`RUN-19`).*\n- The restatement check fails on a deliberately introduced second normative copy, and passes once\n  that copy quotes its owner. Demonstrate both directions; a check that only ever passes tests\n  nothing (`F5`).\n- `check_citations.py` fails on a quote altered by one word. Demonstrate it.\n- Every one of the eight lines flagged today is triaged: home, quote, or fixed.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T02:07:54.897669240Z","created_by":"master","updated_at":"2026-09-22T03:45:52.661619446Z","closed_at":"2026-09-22T03:45:52.661568549Z","close_reason":"Accepted after Round 4: restatement and owner-only citation checks integrated; original probe triaged; all README gates and 41 citation controls pass. Formatting-normalization precision recorded separately; existing lexical limits accepted.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-v8j","title":"Automate and make portable the Panel trace regression checks","description":"Follow-up from the Panel review of rl-panel-membership-in-trace-lhx, Run 20260921T164030.922291158Z-1065755. The membership change has a reproducible conformance/test-panel-trace script, but no automatic gate invokes it. Its scratch omission mutants depend on two literal loops in rloop-bash; the ordinary conformance/run remains implementation-independent.\n\nMake the regression checks routinely runnable without depending on a developer workstation path. Preserve positive complete-Panel controls, actual omitted-call rejection through the scenario comparator, mixed success/timeout membership, actual fake-agent timeouts, and Round isolation. Choose an appropriate automation home without adding an Implementation to this Specification repository or new runtime dependencies to conformance/run. Do not silently skip the controls if an executable or a mutation site is unavailable; make the limitation explicit.\n\nAs part of that work, document a portable invocation and settle whether the script is invoked explicitly through bash or directly; align its mode and examples with that choice. The existing bash invocation works, so these usability improvements do not block the membership prefactor.\n\nAcceptance: a documented automated command runs the regressions, passes the complete-Panel controls, rejects both an omitted Reviewer and a regression to end-only observation, and works without /home/master paths. Retain the existing specification gates and executable conformance checks. No requirement or scenario-alphabet expansion is implied by this follow-up.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T17:25:04.788017795Z","created_by":"assistant","updated_at":"2026-09-23T02:21:53.860490898Z","closed_at":"2026-09-23T02:21:53.860442116Z","close_reason":"Done in 0d0e50d: test-panel-trace runs its comparator checks with no argument as an eighth gate in check-all.sh, NOT RUN line for the executable checks, negative control in gates.yml, direct invocation documented; rloop-bash CI half filed as a follow-up","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"comments":[{"id":2,"issue_id":"rl-v8j","author":"master","text":"Evidence from Run 20260922T214422.432717855Z-3306563: the rot this bead predicts has now happened once and been repaired by hand. test-panel-trace's omit-sol mutation asserts 'grep -c for rv in fable opus astra sol' equals 2; rloop-bash grew a third such loop in its RUN-21 probe reading, so the assertion stopped matching and the control silently stopped exercising what it names. It was rescoped to do_round in 7906921.\n\nThat Run also added eight probe_case mutants to the same script, each pinned to exact rloop-bash source text with 'grep -cF -- \"$site\"' required to equal 1 (the family table rows, the pids+=(\"\") arm, unavail initialisation, the probe exit-status conjunction, the anchored case pattern and the fam fallthrough). They will rot the same way and just as silently, so the surface this bead covers is now larger.\n\nNit from the same Panel, worth folding in: conformance/test-panel-trace line 5 is 123 columns where the file and repo wrap at about 98.","created_at":"2026-09-22T22:28:51Z"}]}
{"id":"rl-probe-uncovered-shapes-gp1","title":"Probe shapes RUN-21 rules on and the suite never produces","description":"Two shapes of probe output `RUN-21` gives a rule for, and `conformance/run` never produces. Both\nwere found by reviewers reading `RUN-21` against `conformance/fakes/agent`, not by a red item —\nthe suite is green over them because the fake cannot express them.\n\n## The two cases\n\n1. **The aggregate line at 100%.** The fake's `clear` shape always writes\n   `Current week (all models): 67% used`, and the `fable`/`opus` shapes write it at 67% too. So\n   `Current week (all models): 100% used` — the likeliest real exhaustion, a whole account out —\n   is never seen. `RUN-21` says it reads `unknown`, deliberately: *\"no family is named in it,\n   nobody has observed what the probe prints when a whole account is exhausted, and a rule written\n   against an unobserved format is a guess.\"* That is a rule, and nothing tests it.\n\n2. **Both families at 100% at once.** `fable` and `opus` are scripted one at a time. Two\n   `unavailable` Reviewers in one Round is reachable in the real world the moment an account's\n   Fable and Opus buckets are both spent, and it exercises what one family cannot: two Reviewers\n   not called, `astra` and `sol` still called, `failed` reaching 2 and the Run still reaching its\n   judge.\n\n## What to change\n\n- `conformance/fakes/agent`: an `aggregate` shape writing `Current week (all models): 100% used`,\n  and a `both` shape writing Fable and Opus at 100% in one output.\n- `CNF-26`: add the aggregate shape to its list of readings that leave every Reviewer `unknown`.\n- `CNF-27`: add the two-unavailable case — `fable` and `opus` neither called and both recorded,\n  `astra` and `sol` both called, the judge reached, the Run ending 0.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, all gates PASS.\n- Each new case fails against an Implementation that gets it wrong. Demonstrate both; a case that\n  passes either way is what this ticket exists to remove.\n- `spec/conformance/run ./result/bin/rloop --self-check` still passes against `rloop-bash`, which\n  already handles both correctly — so these are coverage, not a behaviour change. If either goes\n  red, the Implementation was wrong and the ticket found a real defect.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-21T21:04:39.699659606Z","created_by":"master","updated_at":"2026-09-21T21:42:25.678395766Z","closed_at":"2026-09-21T21:42:25.678343448Z","close_reason":"Accepted Round 1: aggregate and both Probe shapes covered; gates pass, conformance 25/25 with self-check; aggregate mutant fails only CNF-26 and both-family mutant fails only CNF-27. Line-termination follow-up rl-vcq.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-vcq","title":"Clarify ownership of line termination in Probe and not-run records","description":"Follow-up from Opus suggestions 1 and 2 in Round 1 of Run 20260921T212700.418216043Z-2328148, reviewing rl-probe-uncovered-shapes-gp1.\n\nThe existing CNF-27 Feedback File comparison requires a terminating newline and rejects extra blank lines; the accepted coverage task explicitly preserved that convention. Its amended prose now makes the newline explicit. RUN-15 says `holding exactly` followed by `REVIEWER NOT RUN (unavailable)` and `and nothing else`, without explicitly describing the file terminator. Separately, CNF-27 compares probe-1.md byte for byte while CNF-25 and CNF-26 use command substitution, which strips trailing newlines.\n\nAsk the Specification owner to settle the intended byte contract: whether a final newline is required, optional, or otherwise constrained for these records. Recommend making the chosen contract explicit in RUN-15 and RUN-21 as appropriate, and then aligning CNF-25/CNF-26/CNF-27 and their assertions with those owners. Do not resolve the specification question merely by choosing a comparison technique.\n\nAcceptance: the owner's clarification is recorded against the owning identifiers, the affected conformance items agree, and checks demonstrate the chosen behavior for a normal record, a missing final newline, and extra blank lines. All specification gates and the executable Conformance Suite remain green. This follow-up does not change the completed task's two Probe shapes or require an availability scenario axis.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-21T21:41:31.443880944Z","created_by":"assistant","updated_at":"2026-09-23T01:48:20.429836347Z","closed_at":"2026-09-23T01:48:20.429787214Z","close_reason":"Settled by the owner on 2026-09-22 and recorded in rl-f3n's Run: RUN-15's not-run line is followed by a newline and 'and nothing else' means no other content; every line of probe-pick.md and probe-<r>.md ends with a newline (RUN-21). CNF-25/26/27 compare bytes (cmp); scratch mutants without the last newline and with an extra blank line go red (m5, m6, m7).","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-vcq","depends_on_id":"rl-f3n","type":"blocks","created_at":"2026-09-22T21:43:13.763775045Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-bvz","title":"Adopt amended PRM-4 in rloop-bash and pass conformance","description":"Follow-up from rl-prm4-shortcut-swallows-item-0-nyl, accepted in Run 20260922T020308.579277908Z-1183472. This Specification amends PRM-4 to end its report block with: If you have nothing for items 1 and 2, write item 0 and then exactly: No findings.\n\nIn the separate rloop-bash repository, update the hardcoded review prompt to those exact bytes, bump spec/ to the accepted Specification revision containing this amendment, rebuild, and run spec/conformance/run ./result/bin/rloop. Record the Implementation commit, Specification pin, build command, and conformance result. Acceptance is a passing Conformance Suite against the bumped pin. CNF-11/CNF-12 failures against the old hardcoded prompt are expected until adoption; do not weaken the fixture or comparisons. ADR-0001 owns this cross-repository adoption path. Do not add an Implementation to this Specification repository.\n\nThis obligation was not tested or completed by the Specification Run. Model compliance is a separate live observation after adoption.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-22T02:09:56.641249577Z","created_by":"assistant","updated_at":"2026-09-22T02:14:50.810790261Z","closed_at":"2026-09-22T02:14:50.810736871Z","close_reason":"Adopted in rloop-bash d37b128: spec pin bumped to 3da20a8 and the hardcoded review prompt's closing line taken from the regenerated fixture. spec/conformance/run --self-check is 25 passed, 0 failed. Live model compliance is not established by this and remains rl-vgy.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-vgy","title":"Observe clean codex reports after PRM-4 adoption (CNF-22)","description":"Follow-up from rl-prm4-shortcut-swallows-item-0-nyl and Opus suggestions in Run 20260922T020308.579277908Z-1183472. At the next rloop-bash release after adoption of amended PRM-4, observe real codex Reviewers receiving the amended prompt.\n\nPrediction: a clean report includes item 0 (verification limitations, or I ran everything I needed when true), followed by No findings. Record the actual prompt/revision used, Implementation release and commit, Specification pin, CLI/model versions, Run Directory, and Feedback Files. A fixture equality check proves delivery, not compliance. The Panel of the amendment Run produced bare codex reports but does not establish that it received the amended prompt.\n\nRecord the actual outcome as a CNF-22 live observation in 07-open-findings.md, following the existing F entries; allocate the next available identifier at that time, rather than reserving F15 now. If the amended prompt still produces a bare literal, report the failed prediction and raise the proposed PRM-2 follow-up to have the Manager weigh a report missing item 0; do not silently implement that separate amendment. Acceptance is recorded live evidence and explicit disposition of the prediction, not a fabricated green suite item.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-22T02:09:56.675450073Z","created_by":"assistant","updated_at":"2026-09-22T03:48:23.742757719Z","closed_at":"2026-09-22T03:48:23.742704048Z","close_reason":"Observed and recorded as F15. Eight codex reports across four Rounds on rloop-bash d37b128, all carrying item 0, against a baseline of six bare thirteen-byte reports over three Runs. The Round 4 astra report is clean and still carries provenance. The executing store path was verified to contain the amended bytes first, after nix run silently served a cached build on an earlier attempt.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-vgy","depends_on_id":"rl-bvz","type":"blocks","created_at":"2026-09-22T02:09:56.705312548Z","created_by":"assistant","metadata":"{}","thread_id":""}]}
{"id":"rl-gan","title":"Document and test citation normalization boundaries","description":"Follow-up from Sol's Round 4 review of rl-restatement-and-citation-gates-rzt, independently reproduced by the Manager at 3389ac9.\n\nIn tools/spec_text.py, norm() removes asterisks, backticks and tildes but retains underscores. With tools/check_ids.py clause_key(\"RUN-16\", text), \"Manager MUST run\" and \"~~Manager MUST run~~\" have the same fingerprint, while \"_Manager_ MUST run\" differs. F2 describes the normalization as ignoring whitespace and Markdown emphasis, without making this distinction clear.\n\nThis is deferred normalization/documentation precision, not a failure of the delivered restatement-copy/verified-quote acceptance cases. All required mutations and 41 controls passed. Do not reopen the completed gate task solely for this formatting edge.\n\nMake the supported normalization boundary explicit in F2 and the helper documentation, and add focused controls for underscore emphasis and strikethrough. If behavior is adjusted, distinguish markup from underscores/tildes inside literal code or identifiers; do not indiscriminately strip more characters. Preserve owner-only quote verification, clause fingerprint specificity, and all existing gate controls. Do not expand into a general Markdown parser. Acceptance: documented behavior and controls agree for plain prose, underscore emphasis, strikethrough, and literal code characters; the full unpiped gates and citation controls pass.\n","notes":"Settled 2026-09-23 after a four-model bead review: document and test the current behaviour; do not change norm(). F2 and the helper docstring state the exact boundary (asterisks, backticks and tildes are stripped; underscores are kept, so _emphasis_ changes a fingerprint and ~~strikethrough~~ does not), with a positive and a negative control for each of underscore emphasis, strikethrough and literal code characters. A delimiter-aware normalizer is out of scope: it is the general Markdown parser the bead already forbids.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-22T03:45:52.563665369Z","created_by":"assistant","updated_at":"2026-09-24T22:53:29.184396551Z","closed_at":"2026-09-24T22:53:29.184342649Z","close_reason":"Done in Run 20260924T223240.212906000Z-494739, commit 2e72f78: F2 states the normalizer's character rule once (asterisks, backticks, tildes stripped everywhere; underscores kept; whitespace collapsed) with its restatement-homes.json consequence, the helper docstrings point at it, and three controls (strikethrough, underscore emphasis, literal code characters) each red against a scratch mutant of norm(). Panel nits filed as rl-bim.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-gate-scope-boundary-kyx","title":"Split the gates into a blocking and an advisory tier (ADR-0007)","description":"Split the restatement and citation gates into a blocking tier and an advisory tier, as\n`ADR-0007` decides, and state in `F2` which tier each form belongs to.\n\n`ADR-0007` is the decision and owns the reasoning. This bead is the work.\n\n## What blocks the build\n\nExactly these forms, where the ownership claim is explicit in the syntax rather than inferred:\n\n**Every introducer form below binds only an adjacent quotation** — nothing but whitespace\nbetween the introducer and the quote. This applies to forms 1 and 2 exactly as to form 3.\nIntervening prose makes the attribution advisory, whatever punctuation introduced it\n(`ADR-0007`).\n\n1. an identifier followed by a colon;\n2. a possessive — `` `RUN-16`'s `` ;\n3. a quotation immediately after its introducer;\n4. a parenthetical whose **ownership prefix** is identifiers and separators. The ownership\n   prefix is the content before the explanatory marker `F2` already recognises, or the whole\n   content when there is none. So `` (`RUN-16`, `SEQ-4`) `` blocks, and\n   `` (`SEQ-4`; but see `RUN-16` …) `` blocks on `SEQ-4` while the words after the marker claim\n   nothing — which is why the acceptance table below gives that sentence the blocking tier.\n   Do **not** test the whole parenthetical content; that would make the second advisory and\n   contradict the table.\n\n**The enumerated introducing-phrase list starts empty.** `ADR-0007` allows such a list; adding a\nphrase to it is a separate decision recorded in `F2`, not part of this work. Do not invent\nmembers for it.\n\n## What warns and does not block\n\nEverything else the current scanner detects, including ownership inferred across unrestricted\nintervening prose. It still runs and still reports; it does not fail the build.\n\nA finding in this tier must be distinguishable from a blocking one in the output, and the\naggregate runner's exit status must not depend on it.\n\n## Also required\n\n- **Restore `docs/adr/0006-*.md`'s original wording**, `` the probe `AGT-18` states ``, replacing\n  `` the probe from `AGT-18` ``. It was changed in `2e7dfb8` only to stop a gate objecting to\n  correct prose. Confirm it is silent or advisory afterwards, never blocking.\n- Check the rest of the Specification for sentences reworded for the same reason and restore any\n  found. `2e7dfb8` is the commit to read.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with every gate in\n  `README.md`'s Gates table passing.\n- **Each of the four blocking forms has a control that fails when the attribution is false and\n  passes when it is true.** Both directions, or the control proves nothing.\n- **Each advisory form has a control asserting the behaviour that actually occurs** — the finding\n  is reported and the exit status is unaffected. *Do not write a control asserting silence.*\n  Closing one recognition route can re-open another: excluding a parenthetical from the citation\n  route was measured to produce a `RESTATEMENT` finding instead, because `tools/check_ids.py`\n  treats a span as covered only when an owner was found for it. Assert what happens.\n- **A control covers the nearest-citation fallback** in `tools/spec_text.py`: a form the explicit\n  recogniser declines can still be caught by that route, and the tier it lands in must be\n  asserted, not assumed.\n- These sentences, run through both gates, land in the tier `ADR-0007` gives them — verify each,\n  and keep them as controls:\n\n  | sentence | tier |\n  |---|---|\n  | `` `SEQ-4` says the rule is `<RUN-16's exact words>` (`RUN-16`). `` | advisory |\n  | `` `SEQ-4` says nothing about `one session` (`RUN-16`). `` | advisory |\n  | `` `<RUN-16's exact words>` (`SEQ-4`; but see `RUN-16` …). `` | blocking |\n  | `` `<RUN-16's exact words>` (`RUN-16`, `SEQ-4`). `` | blocking |\n  | `ADR-0006`'s restored sentence | not blocking |\n\n- `F2` states which tier each form belongs to and points at `ADR-0007` for why. It does not\n  restate the reasoning.\n- `README.md`'s Gates table says which gates can fail the build and which only report.\n\n## Not in scope\n\nTeaching the scanner to parse relative clauses or any grammar beyond the four blocking forms.\nAdding members to the enumerated introducing-phrase list. Narrowing the quotation-shape heuristic\non the inferred route — worth doing for advisory noise, but it is not this decision and\n`ADR-0007` records why.\n","notes":"Run 20260922T151548.935139763Z-2650415 blocked before a Task File: acceptance says excluded forms are the ones enumerated above, while the decision section asks to choose the boundary. Clarification needed whether all historical escape classes must become silent or some remain supported as F2 currently promises. See the Run finished.md for passages, observable differences, and recommendation.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-22T03:47:51.808409009Z","created_by":"master","updated_at":"2026-09-22T18:23:12.608772984Z","closed_at":"2026-09-22T18:23:12.608720064Z","close_reason":"Implementation accepted after Round 1 of Run 20260922T180203.403531248Z-2897358: both boundary defects repaired; 59 controls and all gates pass. Run blocked solely on caller-owned dirty-path commit prohibition; remaining integration tracked in rl-isk. No work committed.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-isk","title":"Reconcile and commit the accepted gate tier changes","description":"Closeout follow-up for rl-gate-scope-boundary-kyx, Run 20260922T180203.403531248Z-2897358.\n\nThe owner clarified ADR-0007: all quoted introducer forms require whitespace-only adjacency. Round 1 repaired the distant-possessive promotion and sentence-boundary masking defects, aligned F2, and added regression controls. The Manager accepts the implementation against the Task File. All 59 controls, the unpiped aggregate gates, git diff --check and independent disposable-copy probes pass. Astra and Sol reported no findings; Opus verified both repairs. No further code repair or specification clarification is currently required.\n\nThe Run is blocked only on integration authorization. The judge instruction expressly says every dirty-at-start path is not the Manager's to commit; all task-bearing paths, including .beads/issues.jsonl, are on that list. The Manager therefore made no commit. HEAD and review base remain ec6e531b343be55aeb8368a0180086df2addefb4. The accepted prior-Run work, current repairs, owner's ADR clarification and tracker changes remain uncommitted.\n\nThe caller must reconcile and commit the accepted work, or expressly authorize a later Manager to commit these caller-owned changes. Preserve the accepted repairs and original prose restorations. Before integration, rerun nix develop --command bash tools/check-all.sh unpiped, nix develop --command python3 tools/test_citation_gates.py, and git diff --check; flush the Beads export before committing and retain hooks/signing. Close this follow-up when the accepted work and tracker are in history and the tree is clean.\n\nSee this Run's finished.md for the complete Panel disposition, evidence and dirty-path list. This updates the earlier clarification/repair follow-up; those technical questions are now resolved.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-22T17:58:10.896422028Z","created_by":"assistant","updated_at":"2026-09-22T18:26:57.291994557Z","closed_at":"2026-09-22T18:26:57.291946687Z","close_reason":"Committed as ed66a04 by the caller, which PRM-2 required: the work began as caller-owned dirty-at-start paths and was outside the Manager's authority to commit. Seven gates exit 0, 59 controls pass under nix develop, all five pinned sentences verified in their stated tiers.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-f3n","title":"Refuse a Run whose Seat cannot answer (#3, ADR-0008)","description":"Refuse a Run whose Seat cannot answer, and catch a Manager that runs out mid-Run.\n\n`ADR-0008` is the decision and owns the reasoning. This bead is the work. `CONTEXT.md` already\ndefines `Seat`; do not redefine it.\n\n## What to change\n\n**Amend `RUN-21`.** The Probe runs before the pick as well as immediately before each Round's\nPanel. It records one `<seat>:<verdict>` line per Seat — `manager`, `implementer`, `fable`,\n`opus`, `astra`, `sol`, in that order — in `probe-pick.md` before the pick and in `probe-<r>.md`\neach Round. The verdict rule does not change: a Seat is `unavailable` only on the anchored\n`Current week (<family>): 100% used` match with the family in the existing two-row table, and\n`unknown` in every other case. A Seat whose model is outside that table is always `unknown`.\n\n**Add `RUN-22`.** Two rules:\n\n1. When `probe-pick.md` records the Manager's Seat `unavailable`, or the Implementer's Seat\n   `unavailable`, rloop MUST exit 2 without spawning any agent, and MUST write to standard error a\n   message naming the Seat, its model, and the reset the probe reported.\n2. rloop MUST NOT make Round `r`'s judge call when `probe-<r>.md` records the Manager's Seat\n   `unavailable`; it MUST exit 2. This precedes `RUN-11` rather than adding a row to it: that\n   requirement decides `After every Manager call`, and a call that was never made is not one.\n\n**Amend `DIR-4`.** Add `probe-pick.out`, `probe-pick.err` and `probe-pick.md`, written by rloop\nbefore the pick. Restate `probe-<r>.md`'s entry as the per-Seat line.\n\n**Amend `CONTEXT.md`'s `Probe` entry.** It scopes the Probe to a Round's Panel and to Reviewers;\nboth become false. Remove `preflight` from its avoided aliases — `ADR-0008` records why.\n\n**The byte contract, already settled — do not treat it as open.** Every line of `probe-pick.md`\nand `probe-<r>.md` ends with a newline, including the last, and a not-run Feedback File is\n`REVIEWER NOT RUN (unavailable)` followed by a newline. `RUN-15`'s `and nothing else` means no\nother content, not the absence of a terminator; say so where it is read. This is what rloop-bash\nalready writes — a not-run Feedback File measured 31 bytes on 2026-09-22 — so the change is to the\nSpecification and the items below, not to the Implementation. `CNF-25`, `CNF-26` and `CNF-27`\ndisagree about it today: one compares bytes, two use command substitution, which cannot see a\ntrailing newline. Align all three. This closes `rl-vcq`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate in `README.md`'s\n  Gates table PASS.\n- A conformance item for each of the two refusals, the Manager's Seat and the Implementer's Seat.\n  **Each MUST assert that no agent was spawned**, not merely that the status was 2 — a Run that\n  spawned a Manager and failed also exits 2, so a status-only check passes against an\n  Implementation that ignores this entirely.\n- A conformance item for the judge guard: a Run whose Round 2 probe records the Manager's Seat\n  `unavailable` exits 2 with no `manager-judge-2.out` in the Run Directory.\n- A control that a Seat whose model is outside the family table starts the Run, recorded\n  `unknown`. Fail-open is unchanged, and this is what proves it.\n- Every item above fails against an Implementation that gets it wrong. Demonstrate the failure for\n  each; an item that passes both ways tests nothing (`F5`).\n- `CNF-24`–`CNF-27` updated to the per-Seat record and passing.\n\n## Not in scope\n\nSubstituting a model for an unavailable Seat, under any flag — `ADR-0008` rejects it. A second\nProbe guarding the Implementer on every Round; `ADR-0008` records it as available on its own merits\nand not this decision. Validating a model name against a roster: a typo still hangs, and nothing\nhere decides it.\n\n**Do not add a pick-time refusal for an empty Panel.** Four `unavailable` Reviewer verdicts are\nunreachable — two of the four Reviewers are codex and the family table holds no codex model, so\nthey always read `unknown`. `ADR-0008` records why the arm was dropped, and `RUN-15` keeps the\nzero-survivor abort. A conformance item for it cannot be written; do not try.","notes":"Clarification answered by the owner 2026-09-22: reading (2), add --implementer-model with the same per-preset defaults as --manager-model, so every Seat has a model rloop names and the Implementer arm of RUN-22 is reachable. The flag is its own bead, rl-implementer-model-x0u, which this one now depends on; nothing else in this bead changes.","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-22T21:43:11.404012535Z","created_by":"master","updated_at":"2026-09-23T01:48:20.358468313Z","closed_at":"2026-09-23T01:48:20.358420483Z","close_reason":"Accepted after Round 3 (d14d205, e49c026). RUN-21 at two call sites with the six-line per-Seat record; RUN-22 added; RUN-15's byte contract; DIR-4, CONTEXT.md, README. Lean: Seat, seatAvailable, guards pickRefusal/judgeRefusal with witnesses, five RUN-22 theorems, Rloop.decide unchanged; scenarios 227 -> 237, old lines byte-identical apart from the seats column; controls 3 and 2. CNF-3, 9, 24-27 amended; CNF-28-31 new; each red against a scratch mutant (F5). check-all 7/7 PASS; suite 29/29 --self-check against a scratch rloop-bash implementing it; test-panel-trace exit 0. Follow-ups: rl-agt18-probe-seats-po1, rl-seat-items-hygiene-a5t.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-f3n","depends_on_id":"rl-availability-scenario-axis-rnp","type":"blocks","created_at":"2026-09-22T21:43:13.700352375Z","created_by":"master","metadata":"{}","thread_id":""},{"issue_id":"rl-f3n","depends_on_id":"rl-implementer-model-x0u","type":"blocks","created_at":"2026-09-22T23:40:13.696757178Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-adr-0006-untouched-model-stale-tas","title":"ADR-0006's consequence about an untouched model is now false","description":"`ADR-0006`'s first consequence has gone false and needs one sentence corrected.\n\nIt reads:\n\n> **The probe is not an agent.** No prompt is rendered into it, it produces no Feedback File, and\n> it takes no part in the spawn trace the model enumerates — so the formal model and every\n> scenario `conformance/scenarios.tsv` holds are untouched by it.\n\nThe clause up to the dash is still true: the probe is not a spawn and writes no trace line. The\nclause after it is not. `rl-availability-scenario-axis-rnp` gave `Behaviour` an `available` field,\nmade a Panel's spawn name the Reviewers rloop called, and appended twenty-one scenario lines\ncarrying a `probe` column. The formal model and the scenario file are now shaped by the probe's\nverdict, even though the probe itself is still absent from the trace.\n\nAmending an accepted ADR in place is this repository's own practice: `9c28a85`\n(*\"docs: the probe verdict has a consumer now\"*) added a consequence to this same ADR after it was\naccepted on 2026-09-21, when `RUN-15` landed and gave the verdict a consumer.\n\n## What to change\n\nThe sentence above, in `docs/adr/0006-rloop-reads-a-vendors-quota-report-and-fails-open.md`. Keep\nwhat is true — the probe is not in the spawn trace — and say what the verdict now reaches. Nothing\nelse in the ADR is stale; its decision is unchanged.\n\n## Why it was not done in the Run that caused it\n\nThe brief for `rl-availability-scenario-axis-rnp` put requirement and ADR text out of scope and\nauthorised exactly two edits to `06-conformance.md`. The Implementer found this and reported it\nrather than amending, which was correct.\n\n## Acceptance\n\n- The sentence is true again.\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- No requirement text changes; no scenario is regenerated.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-22T22:28:22.583473571Z","created_by":"master","updated_at":"2026-09-23T04:01:18.226322714Z","closed_at":"2026-09-23T04:01:18.226270716Z","close_reason":"Done in ae5e19e: the false clause replaced, verified by the Manager and four Reviewers, gates green.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-probe-shape-fallthrough-2dc","title":"The fake and the model disagree past a probe script's last shape","description":"The fake and the model disagree about what the probe prints in a Round past the last shape a probe\nscript names. Today nothing reaches that Round, so this is a trap for the next probe script rather\nthan a live defect; found by the Panel on Run `20260922T214422.432717855Z-3306563`.\n\n- `conformance/fakes/agent` repeats the **last** shape: `shape=\"${shapes##*|}\"`, and its header\n  comment advertises that — *\"a Round past the last writes the last\"*.\n- `tools/formal/Rloop/Scenarios.lean`'s `behaviour` reads `probes[k - 1]?.getD \"-\"`, and\n  `probeReads \"-\"` is every Reviewer available — i.e. a `clear` probe.\n\nSo a script whose cap exceeds its list of shapes would generate expected traces with a full Panel\nin the later Rounds while the fake kept a Reviewer out of them, and `CNF-3` would go red for a\nreason that is nobody's fault but this mismatch.\n\nIt is unreachable now because every entry of `Scenarios.probeScripts` names one shape per Round its\ncap allows, and `Scenarios.all` filters the block to `(·.rounds.length ≥ probes.length)`.\n\n## What to change\n\nPick one and make it so the other cannot drift:\n\n- the fake falls through to `clear` rather than repeating the last shape, matching the model; or\n- `probeReads`'s docstring (or the enumeration itself) states that a probe script MUST name a shape\n  for every Round its cap allows, and something enforces it.\n\n## Acceptance\n\n- The fake and the model agree on a Round past the last named shape, or that Round cannot be\n  generated.\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- `conformance/run <executable> --self-check` still passes, and the scenario count does not change\n  unless the fix deliberately adds a line, in which case say so.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-22T22:28:42.875281222Z","created_by":"master","updated_at":"2026-09-23T05:09:05.426365901Z","closed_at":"2026-09-23T05:09:05.426316288Z","close_reason":"Closed by commit 3166c52: the fake's probe falls through to clear past a script's last shape, matching the model; regression check in test-panel-trace compares bytes with cmp; follow-ups rl-rounds-fallthrough-a5n and comments on rl-0pj, rl-agents-end-turn-on-background-check-twn","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-probe-block-some-class-1fx","title":"The probe block lacks a Scenario with an uncalled Reviewer and mixed outcomes among the called Reviewers","description":"`Scenarios.narrowProbe` drops the `some` Panel class from all seven probe scripts, but only three\nneed it dropped. Raised by the Panel on Run `20260922T214422.432717855Z-3306563`; **accepted\nhere**, so the Run does not weigh whether to do it. Depends on `rl-rounds-fallthrough-a5n`, whose\nacceptance requires the scenario count unchanged: land after it, then grow the enumeration.\n\nThe stated reason is in the declaration's own docstring: `conformance/fakes/agent` scripts `some`\nas *`fable` alone succeeding*, so with `fable` not called `some` would really be a Panel of\nfailures rather than the class the line names. That bites where `fable` is not called: the\n`fable` script, the `both` script, and Round 1 of the `fable|clear` script (`probeScripts` in\n`tools/formal/Rloop/Scenarios.lean` is the list; see `S225`–`S227`). Under `opus`, `fail`, `loose`\nand `unlisted`, `fable` **is** called, so `some` would enumerate correctly.\n\nWhat the block therefore never reaches: a Scenario with an uncalled Reviewer and mixed outcomes\namong the called ones, whose judge still runs. (`CONTEXT.md`: a Reviewer stays one whether or not\nit was called, so this is about the trace's member list, not a smaller Panel.) The nearby lines\nare close but not the same — `S207`/`S208` are uncalled-and-whole, `S209` uncalled-and-wholly-down.\n\n## What to change\n\n- Change the fake's `some` in `conformance/fakes/agent`'s `reviewer` arm so that `fable` **and\n  `astra`** succeed and `opus` and `sol` fail. A fake process is one Reviewer and cannot know\n  which others were called, so \"the first called Reviewer\" is not something it can compute;\n  `astra` is a codex model, always `unknown` under `RUN-21`'s table and therefore always called,\n  so `some` is a genuinely mixed Panel under every probe script. `CNF-15`'s expectations\n  (`feedback-1-opus.md` fails, `feedback-1-fable.md` is `No findings.`) stay true; check every\n  other hand-written item in `conformance/run` for an assumption that `astra` fails under\n  `panel=some` and adjust it in the same change if one exists.\n- Remove the `some` exclusion from `Scenarios.narrowProbe` and update its docstring; the model's\n  `some` class already means \"a called Reviewer failed, another succeeded\". Say the count before\n  and after in `F4`, which owns the enumeration's size, and keep the block's rationale there.\n  (`rl-cnf32-f4-wording-gke` also edits F4's closing paragraphs; whichever lands second rebases.)\n- Other open beads touch the fake's header; rebase the header, not the logic.\n\n## Acceptance\n\n- The uncovered combination above is a generated line, and it goes red against a scratch mutant\n  of rloop-bash `02c6206`'s `bin/rloop` (`/home/master/p/rloop-bash`, github:douglaz/rloop-bash)\n  that exits 2 without a judge whenever a Round has both an uncalled Reviewer and a failed called\n  one; the line requires the surviving Reviewer to be kept and the judge reached. Demonstrated,\n  not asserted (`F5`).\n- Every pre-existing scenario line is unchanged apart from its identifier if the block grows in\n  the middle; `tools/check_scenarios.py` reports nothing to write after regeneration.\n- `CNF-15` still passes against rloop-bash `02c6206`.\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS, and the new\n  count is reported with the before and after.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-22T22:28:42.930093877Z","created_by":"master","updated_at":"2026-09-25T08:36:23.017737064Z","closed_at":"2026-09-25T08:36:23.017687170Z","close_reason":"The probe block now enumerates the some Panel class in all seven scripts: 237 -> 256 scenarios, 19 pure insertions, every pre-existing line unchanged apart from its identifier. The uncovered combination is generated (S209/S210, S214/S215, S219/S220, S242-S246) and demonstrated red against a scratch mutant of rloop-bash 02c6206 that reads a Reviewer not run beside a called one that failed as an all-down Panel, with the diagnostic asserted byte for byte. CNF-15 green before and after; the suite's verdict against 02c6206 is unchanged (19 passed, 11 failed, the same ids). check-all.sh exit 0, every gate PASS. Panel nits filed as rl-probe-some-panel-nits-0ai.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-probe-block-some-class-1fx","depends_on_id":"rl-rounds-fallthrough-a5n","type":"blocks","created_at":"2026-09-23T16:36:04.912458197Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-8oh","title":"Render unavailable adviser models into the Manager's prompts (#3)","description":"Tell the Manager which adviser models cannot answer, so a Consultation stops paying a bound to\nfind out.\n\nOn 2026-09-22 a Run's Consultation called `claude-fable-5-1`, which was at 100% of its weekly\nlimit, and lost it at its 600s bound with zero bytes on standard output. rloop knew: the Probe had\nread the same quota report seconds earlier. The Manager did not, because nothing carries the\nverdict into its prompt.\n\nrloop does not run the Consultation — `CONTEXT.md` settles that it is `Not a Panel: the Manager\nruns it, rloop does not` — so rloop states the fact and the Manager decides, exactly as\n`{{DIRTY_AT_START}}` already does with the caller's paths. This bead adds no new kind of thing to\na prompt.\n\nDepends on `rl-f3n`: the render needs a Probe that has run before the pick.\n\n## What to change\n\n**Amend `PRM-1`.** Add a `{{UNAVAILABLE}}` render naming every Reviewer model the pick Probe\nrecorded `unavailable`, one per line, and nothing at all when none was — the same empty form\n`{{DIRTY_AT_START}}` already uses. The Manager's Seat and the Implementer's Seat never appear:\n`RUN-22` refuses the Run outright in those cases, so no prompt is ever rendered with one.\n\nAmend step 4's second bullet, which already reads `skipping any model that did not answer earlier\nin this Run and using its eligible counterpart instead`, so that a model this prompt lists is\nskipped on the same terms and for the same reason.\n\n**Amend `PRM-2`.** No new render: the judge prompt already states that a Feedback File holding\n`REVIEWER NOT RUN` is `a Reviewer that was never called, because its model could not answer`. Add\none sentence making that file a reason to skip the model as an adviser for **that Round's**\nConsultation. Key it on the Round's own Feedback Files, never on the pick — a weekly limit resets\nat a fixed time and an Implementer may run for hours, so the pick's reading is the stale one by\nthen.\n\n**Amend `RUN-20`.** An adviser whose model the relevant Probe recorded `unavailable` MUST be\ntreated as not having answered, without being called, and replaced by its counterpart under the\nrule already there: `The Manager MUST replace an adviser that did not answer by its counterpart in\nthe other pair: `fable`↔`opus`, `astra`↔`sol`, not by roster order.` This is the Consultation's\nparallel to what `RUN-15` does for the Panel, where a Reviewer recorded unavailable `MUST NOT be\ncalled at all`.\n\n**Regenerate the fixtures** with `python3 tools/check_fixtures.py --write`. Never edit\n`conformance/fixtures/` by hand.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate in `README.md`'s\n  Gates table PASS.\n- A conformance item comparing the rendered pick prompt byte for byte in **both** states: a pick\n  Probe that recorded a Reviewer unavailable, and one that recorded none. A test of only the\n  populated form passes against an Implementation that renders the list unconditionally, and a\n  test of only the empty form passes against one that never renders it at all.\n- The empty form is specified, not left to the Implementation, and its bytes are what the item\n  compares.\n- Both items fail against an Implementation that gets them wrong; demonstrate each failure (`F5`).\n- No fixture under `conformance/fixtures/` is hand-edited; the tool wrote every changed byte.\n\n## Not in scope\n\nHaving rloop call the advisers, or choose them. Probing the codex advisers — that vendor publishes\nno quota, `astra` and `sol` always read `unknown`, and nothing here changes that. Adding an adviser\noutside `RUN-20`'s two pairs. Any change to what counts as an answer from an adviser that *was*\ncalled.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-22T23:24:23.840481418Z","created_by":"master","updated_at":"2026-09-23T03:53:05.323271107Z","closed_at":"2026-09-23T03:53:05.323221604Z","close_reason":"Accepted after Round 3 (80977b0, 6d26c10). PRM-1 gains {{UNAVAILABLE}} (bare Seat name per line, RUN-21 order, empty string when none — settled by Consultation, fable and astra agreeing on the form), step 4 skips a listed model; PRM-2 reads the same off REVIEWER NOT RUN Feedback Files; PRM-6 row; RUN-20 treats a Probe-unavailable adviser as one that did not answer, without a call; CNF-12 amended, CNF-32 new (four Runs: opus, fable, fable+opus, none, each byte for byte and each checked to start); F12 dated paragraph; README clause; fixtures regenerated by the tool. check-all 8/8 PASS, test_citation_gates 59 OK, scenarios unchanged at 237, no Lean change. Suite against scratch u1 (m1 + render): 30/30; mutants u2 (unconditional) red CNF-11/12/32, u3 (never) and u4 (space-joined) red CNF-32, u5 (refuses on opus) red CNF-31/32, u6 (drops fable alone) red CNF-32 fable Run only. Correction to this bead's own text: no probe-pick.md existed on 2026-09-22 — that Implementation had no pick Probe; the verdict was on disk 17 minutes later in probe-1.md (F12 now says so). Follow-ups: rl-prompt-record-skipped-adviser-eue, rl-cnf32-f4-wording-gke, rl-agents-end-turn-on-background-check-twn.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-8oh","depends_on_id":"rl-f3n","type":"blocks","created_at":"2026-09-22T23:24:25.993196260Z","created_by":"master","metadata":"{}","thread_id":""}]}
{"id":"rl-implementer-model-x0u","title":"Add --implementer-model so the Implementer's Seat has a model rloop names (#3, ADR-0008)","description":"Give the Implementer's Seat a model rloop can name: add `--implementer-model MODEL`.\n\nA Run on 2026-09-22 (`20260922T233436.601132825Z-497547`) blocked at the pick on `rl-f3n`:\n`ADR-0008` refuses a Run when the Implementer's Seat is `unavailable`, but `AGT-5`/`AGT-6` say the\nImplementer command lines carry no model flag and `the CLI's configured default is the\nImplementer's model`. rloop cannot name a model it never chose, so that Seat would always read\n`unknown` and the refusal arm could never fire.\n\nThe owner settled it the way the Manager recommended: the flag. `ADR-0008` was decided as \"both\nSeats, one rule\" and dropping the Implementer arm would gut that decision. This bead is only the\nflag; `rl-f3n` stays the Probe-and-refuse work and is blocked on this one.\n\n## What to change\n\n**Amend `AGT-1`.** Add, directly under `--manager-model`:\n\n```\n  --implementer-model MODEL    the Implementer's model                   (default: the preset's, AGT-5)\n```\n\n**Amend `AGT-5` and `AGT-6`.** The claude Implementer command line gains\n`--model <implementer model>`; the codex one gains `-m <implementer model>`. Place each where\n`AGT-3`/`AGT-4` place the Manager's equivalent, so the two Seats read alike. `<implementer model>`\nis `--implementer-model`'s value, which defaults to `claude-fable-5-1` under `--implementer claude`\nand to `gpt-6-astra` under `--implementer codex`, the same defaults `AGT-3` gives the Manager.\nWithdraw the sentence `Neither carries a model flag: the CLI's configured default is the\nImplementer's model.` and say in an italic note why it changed and when (2026-09-22), including the\nlive cost: an operator whose CLI default is not `claude-fable-5-1` now gets that model unless they\npass the flag.\n\n**`AGT-2`** already covers the new flag under its missing-value rule; confirm and do not restate.\n\n**Regenerate the argv fixtures** with `python3 tools/check_fixtures.py --write`. Never edit\n`conformance/fixtures/` by hand.\n\n**Extend the conformance items** that compare the Implementer's argument list so they see the new\nargument under both presets, and add one for a non-default `--implementer-model` value reaching\nthe Implementer's argv.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate in `README.md`'s\n  Gates table PASS.\n- The Implementer argv fixtures for both presets carry the model argument at the specified\n  position, written by the tool.\n- A conformance item fails against an Implementation that ignores `--implementer-model` and\n  spawns without the argument; demonstrate the failure (`F5`).\n- `CONTEXT.md`'s `Seat` entry (`Each seat has its own model`) is now true of every Seat without\n  edit.\n\n## Not in scope\n\nThe Probe, `RUN-22`, or any refusal — that is `rl-f3n`. Validating the model name against a\nroster. Reading the CLI's configured default: rloop chooses the model or the operator does; nothing\nasks the CLI.\n","status":"closed","priority":1,"issue_type":"task","created_at":"2026-09-22T23:40:08.424613206Z","created_by":"master","updated_at":"2026-09-23T00:03:36.318013991Z","closed_at":"2026-09-23T00:03:36.317964177Z","close_reason":"Landed in 6bbac9f: AGT-1, AGT-5, AGT-6 amended, fixtures regenerated by the tool, CNF-11 asserts three cases; all gates green, F5 shown red/green on scratch mutants, Panel of four: no findings","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-aa4","title":"conformance/run: the fourth CNF-11 Run resets three P_* variables and asserts one Seat","description":"Follow-up from the Panel of Run 20260922T234020.230261322Z-505649 (rl-implementer-model-x0u,\ncommit 6bbac9f). Nothing is red today; each item is a trap for the next edit.\n\nThe fourth Run in conformance/run's CNF-11 block (--implementer-model claude-opus-5):\n\n1. Resets only P_TASK_FILE, P_RUN_DIR and P_UNAVAILABLE. P_BASE, P_FINISHED_FILE,\n   P_IMPLEMENTER_LOG and P_FEEDBACK_FILES still hold the third Run's paths. It passes only\n   because PRM-3 renders {{TASK_FILE}} and {{RUN_DIR}} and nothing else; a third placeholder in\n   PRM-3 turns CNF-11 red on a correct Implementation with a misleading 'argv differs'. Reset the\n   block the way Runs 2 and 3 do, or say in a comment which placeholders PRM-3 uses.\n2. Its check_call writes prompt_ok/prompt_detail after prompts_ok was snapshotted, so its prompt\n   result is dropped silently. Deliberate — the snapshot is taken after the third Run and\n   CNF-12's values are all covered by then — but a fifth Run would inherit the same hole; move\n   the CNF-12 verdict above the fourth Run or say so in code. (rl-prm6-instruction-literal-gnt\n   adds a Run to the same block and must not fall into this hole; whichever lands second\n   rebases.)\n3. `e=` is assigned and never read; every other Run checks or saves its exit. Assert [ \"$e\" = 0 ]\n   or drop the assignment.\n4. It asserts only the Implementer's argv. An Implementation that let --implementer-model also\n   move the Manager's model (or a Reviewer's fixed one) passes. Add to the fourth Run\n   check_calls on both Manager calls, the pick and the judge, with claude-fable-5-1 (CNF-11\n   already owns it: '<manager model> is --manager-model's value, or the preset's default') and\n   on the four Reviewers' fixed argument lists. Needs the P_* reset from item 1. Each new assertion is demonstrated\n   against its own scratch mutant — one that honours --implementer-model but leaks its value into\n   the Manager's call, one that leaks it into a Reviewer's; the existing ignore-the-flag mutant\n   proves existing coverage, not these.\n\nAlso noted, take or leave: expected_argv aliases <manager model> and <implementer model> to one\nargument (sound while no fixture carries both, and the comment says so); AGT-6's withdrawal note\nparaphrases where RUN-16's quotes the withdrawn words verbatim.\n\nThe Implementation used for `F5` demonstrations is /home/master/p/rloop-bash\n(github:douglaz/rloop-bash) at 02c6206, which already carries the flag; nix build there and use\n./result/bin/rloop, or a scratch copy of bin/rloop under /tmp.\n\nAcceptance: the four items addressed in conformance/run; every CNF item still green against\nrloop-bash 02c6206's result/bin/rloop; the suite red against the two leak mutants above and\nagainst a scratch mutant that ignores --implementer-model (F5).\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T00:03:36.258675096Z","created_by":"master","updated_at":"2026-09-25T07:31:22.819900485Z","closed_at":"2026-09-25T07:31:22.819844019Z","close_reason":"Closed by hand 2026-09-25 after Run 20260925T034709.676726212Z-1798353's Round 2 judge hung at its bound with empty streams (session window capped). Round 2 delivered the brief: astra and fable no findings, sol REVIEWER FAILED (exit 1), opus one finding (the comment dropped the off_table_model breadcrumb) applied by hand in the closing commit; gates 8/8. Opus's three suggestions are rl-aa4-nits.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"comments":[{"id":6,"issue_id":"rl-aa4","author":"master","text":"From the Panel of Run 20260924T162203.572447213Z-4040131 (rl-prm6-instruction-literal-gnt, commit 70648f5), three more items in the same CNF-11/CNF-12 block, all cosmetic today:\n5. The hostile Run (rec-argv-hostile) assigns e= and never reads it, the same shape as item 3; sweep both.\n6. The fourth Run's unreset P_INSTRUCTION, P_FINISHED_FILE, P_BASE, P_IMPLEMENTER_LOG and P_FEEDBACK_FILES now inherit the hostile Run's values (braces, a backslash) instead of the third Run's benign ones; harmless while PRM-3 renders only TASK_FILE and RUN_DIR, but item 1's reset matters more now. Note also that render() no longer expands every P_* on each call, so an unset one no longer trips set -u: a missing reset fails quietly.\n7. The Implementer and judge check_call labels say 'hostile instruction' but PRM-2 and PRM-3 carry no {{INSTRUCTION}}; those checks exercise the cyclic Run Directory path alone. Relabel, and CNF-12's prose ('its pick, Implementer and judge prompts equal their fixtures with each value's bytes unchanged') could say the instruction is the pick's alone.","created_at":"2026-09-24T16:54:59Z"}]}
{"id":"rl-agt18-probe-seats-po1","title":"AGT-18's prose covers two Reviewers where the Probe now answers for every Seat","description":"Follow-up from the Panel of Run 20260923T000410.394429023Z-505649 (rl-f3n). Nothing is red.\n\n`AGT-18`'s prose says of the Probe: `One invocation covers both claude Reviewers, because it\nreports every claude family at once`. Since `RUN-21` gained the pick call site and the per-Seat\nrecord, one invocation also answers for the Manager's and the Implementer's Seats. The command\nline does not change and neither does the fixture; the sentence is narrower than what the call\nnow does.\n\nAmend the sentence to: `One invocation serves every Seat, whichever model each holds, because it\nreports every claude family at once and answers even through a model that is itself exhausted;\nwhat each Seat then reads is RUN-21's.` Not \"every Seat whose model is a claude model\":\n`claude-sonnet-5` is a claude model off `RUN-21`'s table and reads `unknown` whatever the probe\nwrote, so the probe does not answer for it. Do not restate `RUN-21`'s rule: cite it.\n`python3 tools/check_fixtures.py` must stay green (the block is untouched).\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; `AGT-18`'s prose\nnames the Seats, not the two Reviewers, and is consistent with `RUN-21`'s off-table rule; no\nfixture changes.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T00:51:14.073539756Z","created_by":"master","updated_at":"2026-09-25T07:36:44.894597372Z","closed_at":"2026-09-25T07:36:44.894543271Z","close_reason":"done","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-seat-items-hygiene-a5t","title":"Seat items: record_is stderr leak, dashless flag hole, Opus rows for CNF-29/CNF-30","description":"Follow-up from the Panel of Run 20260923T000410.394429023Z-505649 (rl-f3n, commits d14d205 and\ne49c026). Nothing is red; every item is a trap or a gap in what the Seat items assert.\n\n1. `record_is` in conformance/run leaks bash's redirection error. Its detail clause runs\n   `$(tr '\\n' ' ' < \"$f\" 2>/dev/null)`, with the `2>/dev/null` after the `<`, so when the record\n   file is missing bash reports it to the suite's stderr before the redirect applies. Against an\n   Implementation without the pick Probe (`rloop-bash` d37b128) the suite prints 27 such lines\n   between its verdicts. Verdicts are unaffected. Fix: `tr '\\n' ' ' 2>/dev/null < \"$f\"`.\n   Round 3 fixed the same class in CNF-24's argv loop and the brief held this one out of scope.\n2. `names` in conformance/run deletes `--manager-model` and `--implementer-model` before matching\n   a Seat's word with `grep -w`, but `-w` treats `-` as a word boundary, so a message that writes\n   the flag without its dashes (`the manager-model flag`) still satisfies `manager` while naming\n   no Seat. `sed -E 's/-*(manager|implementer)-model//g'` closes it at the same cost.\n3. The `Opus` row is asserted for the Manager's Seat at the pick (CNF-28) and for a Reviewer\n   (CNF-25, CNF-27) but not for the Implementer's pick refusal (CNF-29, Fable only) or the\n   Manager's withheld judge (CNF-30, Fable only). Sol built two scratch mutants — one treating an\n   Opus Implementer as off the table, one ignoring Opus for the Manager during Round probes only —\n   and both passed all 29 items. A single table lookup shared by every Seat (what m1 does) cannot\n   fail this way, which is why the Round-3 Manager judged it a follow-up rather than a Round; an\n   Implementation with per-Seat or per-site lookups can. If it is worth closing, one Run each in\n   CNF-29 and CNF-30 with `--implementer-model claude-opus-5` / `--manager-model claude-opus-5`\n   under the `opus` shape does it, and F4's block need not grow (the model reads the Seats through\n   one `seatAvailable`; `S230` already proves the row for the model).\n4. CNF-29 could assert that the Seat that can answer is NOT named: `RUN-22` says the message\n   names `the Seat — both, when both are`, and an Implementation that always names both passes\n   today. Check m1's standard error is free of the word first.\n5. Prose nit, 06-conformance.md CNF-28: \"the Seat's word stands whole in what is left of it\" —\n   the nearest antecedent of \"it\" is the word; say \"in what is left of standard error\".\n6. 01-run-lifecycle.md RUN-7 item 6 is a 113-column line where the list wraps at ~100; wrapping\n   it moves RUN-16's advisory line, which tools/test_citation_gates.py pins by number (248).\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; `conformance/run\n<m1> --self-check` stays 29/29 against a scratch copy of rloop-bash implementing RUN-22; each new\nassertion demonstrated red against a scratch mutant (F5); no requirement changes.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T01:45:00.798425731Z","created_by":"master","updated_at":"2026-09-23T09:22:15.142873419Z","closed_at":"2026-09-23T09:22:15.142817735Z","close_reason":"Closed by hand 2026-09-23 after Run 20260923T062347.389579851Z-505649 lost its Round-1 judge call at the 3600s bound with zero bytes on both streams (fable answered instantly afterwards; probably the 5-hour session window, which RUN-21's weekly anchor does not see). The Round's three commits 76f12fa, 9783af4, 789b9c7 stand: gates 8/8 PASS at HEAD; astra, sol and fable found nothing, fable verified every brief item with mutants. Opus's one finding, not_named reads the whole stderr which RUN-10 leaves unconstrained, is the owner's call and is filed as its own bead.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-q40","title":"rloop-bash: run spec/conformance/test-panel-trace ./result/bin/rloop in CI, after catching up with the spec","description":"Follow-up from rl-v8j (spec commit 0d0e50d, Run 20260923T014930.607985846Z-505649). Option A, settled by Consultation, put the executable-dependent checks of conformance/test-panel-trace — the fake-agent timeout, the positive scenario replays and the eight probe_case mutants plus the two omit-sol mutants, all pinned to rloop-bash's source text — where the executable and the source live: rloop-bash's own CI. The spec's check-all.sh now runs only the executable-free comparator checks and prints one NOT RUN line for the rest. Until rloop-bash's ci.yml runs 'spec/conformance/test-panel-trace ./result/bin/rloop' next to its existing 'spec/conformance/run ./result/bin/rloop --self-check' line, the source pins can still rot silently, which is the rot rl-v8j was filed for; this bead is the load-bearing half.\n\nBlocked in practice on rloop-bash catching up with the spec: at d37b128 it has no --implementer-model, no pick Probe and a four-line record, so conformance/run fails 11 items and test-panel-trace's first positive control fails with 'got exit 2 trace ''. /tmp/rl-f3n/m1.diff (scratch, from Run 20260923T000410) is a working 60-line implementation against its bin/rloop. Bump the spec submodule to at least 0d0e50d.\n\nAcceptance: rloop-bash CI green with both lines; a deliberate rename of one mutation site (for example ' opus)  fam=Opus ;;' with two spaces) makes the CI red on the site assertion.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T02:21:53.763529741Z","created_by":"master","updated_at":"2026-09-23T15:09:21.999466880Z","closed_at":"2026-09-23T15:09:21.999415804Z","close_reason":"rloop-bash master 08c2c88 (pin dd6d522) runs spec/conformance/test-panel-trace ./result/bin/rloop in CI after the self-check (rloop-bash b496bbc), implemented by an rloop Run on 2026-09-23; suite 30/30, trace exit 0 on a fresh nix build.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-0pj","title":"gates.yml negative controls prove red, not red for the stated reason","description":"Suggestion from opus on the Panel of Run 20260923T014930.607985846Z-505649 (rl-v8j). Every 'Prove the … gate can fail' step in .github/workflows/gates.yml (lines 59–64 as of f0d1154) accepts any non-zero exit from the mutated scratch copy. If a gate broke on ubuntu's bash/mawk/coreutils for an unrelated reason, the step would still pass; the panel-trace step is the only place test-panel-trace runs outside nix develop, so it is the most exposed. Tightening, per step, both halves (ADR-0007: a control asserts concrete behaviour and pairs it with a positive case): run the unmutated scratch copy green first, then require the mutated gate to exit non-zero AND its output to contain the stable diagnostic that mutation causes (for panel-trace: 'FAIL: mixed success and timeout'; for the probe-fallthrough mutant and the rounds-fallthrough one rl-rounds-fallthrough-a5n adds, an equally specific line). Whole-workflow, low priority.\n\nDepends on rl-rounds-fallthrough-a5n, which adds a check (and its own control) to the same block of test-panel-trace and gates.yml; this bead's tightening must cover that new check too, so land after it. rl-5mk edits the last step of the same workflow and should land after this one.\n\nTwo cosmetic nits from the same Panel, worth folding in when touching the script: conformance/test-panel-trace canonicalises the argument before testing it, so a missing directory prints bash's cd error above the 'not executable:' line (test [ -f \"$1\" ] && [ -x \"$1\" ] first; the mktemp -d is at line 21 as of f0d1154); lines 40, 143, 183 and 193 are 99–101 columns and are neither sed expressions nor probe_case arguments (216, 219 and 225 are probe_case arguments and excluded). Line numbers move; re-derive with awk 'length>98' before editing.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T02:21:53.819513281Z","created_by":"master","updated_at":"2026-09-25T09:06:53.861460131Z","closed_at":"2026-09-25T09:06:53.861411149Z","close_reason":"Landed in bb9aca3, 325337c and 64102ff: every gates.yml control runs its gate green on a scratch copy first, then requires non-zero and the mutation's own diagnostic; two new controls cover the fake's probe and Rounds fallthroughs; the three test-panel-trace nits and the README controls sentence are fixed.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-0pj","depends_on_id":"rl-rounds-fallthrough-a5n","type":"blocks","created_at":"2026-09-23T16:36:04.783370424Z","created_by":"master","metadata":"{}","thread_id":""}],"comments":[{"id":4,"issue_id":"rl-0pj","author":"master","text":"Two more from the Panel and the Manager of Run 20260923T042304.628017728Z-505649 (rl-probe-shape-fallthrough-2dc, commit 3166c52): (a) gates.yml's panel-trace negative control (lines 59-64) breaks only scenario-lib.sh's comparator; test-panel-trace now also checks the fake's probe past a script's last shape (README's Gates row says so), and no CI step proves that half can go red — restore the old fallthrough (`|| shape=\"${shapes##*|}\"`) in a scratch copy of conformance/fakes/agent and require exit 1. (b) test-panel-trace's `work=\"$(mktemp -d …)\"` at line 20 has no `|| exit`; when the Manager's gate run hit a nix-shell TMPDIR that had vanished mid-run, the script went on with `work` empty, printed \"mkdir: cannot create directory '/fakes': Permission denied\" and failed on the cp's `|| exit 1` — red, correctly, but for a reason the output does not name. Fail at the mktemp with its own message.\n","created_at":"2026-09-23T05:08:00Z"}]}
{"id":"rl-prompt-record-skipped-adviser-eue","title":"PRM-1 and PRM-2's record sentence does not cover an adviser skipped for its Probe verdict","description":"Follow-up from the Panel of Run 20260923T022252.771492072Z-505649 (rl-8oh, commits 80977b0 and\n6d26c10). Nothing is red.\n\n`RUN-20` now says `An adviser skipped for its Probe verdict is recorded as one that did not\nanswer, with that verdict as the reason.` But `PRM-1`'s record sentence tells the Manager to\nrecord `every model called and what it did`, and `PRM-2`'s says `every model you called and\nwhether it answered`. An adviser skipped for a `{{UNAVAILABLE}}` line or a `REVIEWER NOT RUN`\nFeedback File was never called, so a Manager reading either prompt literally records nothing for\nit, and the record `RUN-20` requires is silently incomplete.\n\nAmend the record sentence in both prompt blocks (`03-prompts.md`) so a skipped adviser is\nrecorded too, with the Probe verdict as its reason; regenerate the fixtures with\n`python3 tools/check_fixtures.py --write`. No requirement change: `RUN-20` already says it.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; only\n`conformance/fixtures/prompts/PRM-1.txt` and `PRM-2.txt` change, by the tool; `CNF-12` and\n`CNF-32` still pass against a scratch executable carrying the new bytes (`/tmp/rl-8oh/u1` is\nthe current one, built from `/tmp/rl-f3n/m1`).\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T03:53:05.182581469Z","created_by":"master","updated_at":"2026-09-23T04:19:36.329080675Z","closed_at":"2026-09-23T04:19:36.329029498Z","close_reason":"Done in ef321aa: PRM-1 and PRM-2 record a skipped adviser with its Probe verdict as the reason; fixtures regenerated; F12 noted. All four Reviewers: no implementation findings.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf32-f4-wording-gke","title":"CNF-32 and F4 wording nits from the rl-8oh Panels","description":"Suggestions from the Panels of Run 20260923T022252.771492072Z-505649 (rl-8oh). Nothing is red;\neach is a wording or width nit in what the Round accepted.\n\n1. `07-open-findings.md` F4 says a Seat-reading leak `is caught only by the block and by CNF-25\n   to CNF-31`. Add `CNF-32` to the list. Its four Runs each pass through `started()`\n   (`conformance/run`), which asserts the pick was called and the Run ended 0 on the default\n   models — a Manager's and Implementer's Seat reading, which is what F4's sentence is about. The\n   render is the extra thing it catches, not the only one. (The mutants that first showed it were\n   rl-8oh's scratch copies and are gone; re-derive from the item if it matters.)\n2. `06-conformance.md` CNF-32's last line is ~121 columns; the citation tuple was left on the\n   wrapped rationale's last line. Re-wrap.\n3. CNF-32's rationale says `one row cannot tell a working render from one that hardcodes a Seat`;\n   in CNF-25 and CNF-28 \"row\" is a row of RUN-21's table, here the varying thing is a Run. Say\n   \"one Run\".\n4. Only CNF-32's `opus` sentence says the Run starts; `conformance/run` applies `started()` to\n   all four Runs. Say it once for all four.\n\nItem 1 and `rl-probe-block-some-class-1fx` both edit F4's closing paragraphs; whichever lands\nsecond rebases.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; no requirement or\nsuite behaviour change; `conformance/run /home/master/p/rloop-bash/result/bin/rloop --self-check`\n(github:douglaz/rloop-bash at `02c6206`, built with `nix build`) exits 0 with every emitted item\nPASS (cite the list, do not copy its count).\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T03:53:05.236065550Z","created_by":"master","updated_at":"2026-09-25T07:47:35.468061449Z","closed_at":"2026-09-25T07:47:35.468011515Z","close_reason":"Four wording and width nits landed in dc5244b: F4's range names CNF-32; CNF-32 re-wrapped inside 100 columns; 'one row' is 'one Run' with the row left to CNF-25 and CNF-28; all four Runs are said to start once. Gates green unpiped; four Reviewers, no findings. Their two remaining wording suggestions are rl-cnf32-starts-ends0-nits-hur.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-agents-end-turn-on-background-check-twn","title":"Headless agents end their turn while a background check runs: two Round losses in one Run","description":"Observed twice in Run 20260923T022252.771492072Z-505649 (rl-8oh), under `--implementer claude`\nand the claude Reviewers, harness version per `AGT-17`:\n\n- Round 1's Implementer ended its turn with `implementer-1.out` holding one line, `Still running\n  u2. I'll be re-invoked when it finishes.` — it had started a suite run in its tool's\n  background and returned; nothing re-invoked it. Its F5 record was left half-written, its\n  report unwritten and its work uncommitted, and a gate regression it would have caught went\n  to the Panel instead.\n- Round 2's `fable` Reviewer wrote `feedback-2-fable.md` as one line, `Waiting on the background\n  runs. There is nothing else independent to fetch…`, and ended: a Panel seat spent, no review.\n\nBoth are the same failure: a headless agent treats its shell tool's background facility as a\npromise to be resumed, and `claude -p` resumes nothing. The Round 2 and Round 3 briefs worked\naround it with `Do not end your turn while a check is still running in the background; wait for\nit and report its result.`, and both Rounds' Implementers complied.\n\nDecide whether `PRM-3` and `PRM-4` should carry that sentence (a prompt change: `03-prompts.md`,\nfixtures regenerated by the tool, `F12`-style note of why), or whether it stays the Manager's to\nput in each brief. `00-overview.md` says the prompts are checked for equality and never for\nquality; this is the first observation that a prompt's silence costs a Round.\n\nAcceptance, if taken: the sentence in both blocks; `nix develop --command bash\ntools/check-all.sh` exits 0, unpiped; only `PRM-3.txt` and `PRM-4.txt` fixtures change, by the\ntool.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T03:53:05.278150387Z","created_by":"master","updated_at":"2026-09-23T05:36:13.913766312Z","closed_at":"2026-09-23T05:36:13.913719303Z","close_reason":"Done in f058880 and 0567f2b: PRM-3 and PRM-4 carry the foreground-only sentence, fixtures regenerated, F16 records the three incidents (open until a live Run shows the sentence working as prompt text)","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"comments":[{"id":3,"issue_id":"rl-agents-end-turn-on-background-check-twn","author":"master","text":"Third occurrence, Run 20260923T042304.628017728Z-505649 (rl-probe-shape-fallthrough-2dc), --implementer claude: Round 1's Implementer ended its turn with implementer-1.out holding one line, \"Both suite runs are still going; I'll pick up when they report.\" — although the brief carried the rl-8oh sentence verbatim (\"Do not end your turn while a check is still running in the background; wait for it and report its result.\"). Its work was complete in the tree but uncommitted and unreported; a Round was spent to commit it. Round 2's brief said instead \"Run every command in the foreground and wait for it. Do not use your shell tool's background facility for any check\", and that Implementer complied. The mitigating sentence is not enough on its own; forbidding the background facility outright is what worked. Weigh that wording for PRM-3/PRM-4.\n","created_at":"2026-09-23T05:08:00Z"}]}
{"id":"rl-bs8","title":"ADR-0006 first consequence: name the judge refusal; 'seat' carries the seating, not the verdict","description":"Suggestions from the Panel of Run 20260923T035349.764605558Z-505649 (rl-adr-0006-untouched-model-stale-tas, commit ae5e19e). Nothing is red; the bullet is true as written and every Reviewer said so.\n\nThe corrected first bullet under *Consequences* in docs/adr/0006-rloop-reads-a-vendors-quota-report-and-fails-open.md names two of the three paths the Probe's verdict takes through the model, and attributes the verdict to two fields where one holds it:\n\n1. The judge refusal is unnamed. `rounds` in tools/formal/Rloop/Loop.lean withholds the judge call when the Manager's Seat is `unavailable` before a Round's Panel (the `g.judgeRefusal` branch, RUN-22); scenario rows such as S233 script it (trace ending at `panel2`, no `judge2`). The bullet names the Panel omission and the pick refusal only. A reader auditing \"its verdict reaches the model\" against scenarios.tsv finds S233 unaccounted for. One clause closes it.\n2. \"`Behaviour`'s `available` and `seat` fields carry it\": per Loop.lean's doc comment, `available` is the verdict and `seat s` is where Seat s's model stands in RUN-21's table (none when the table does not name it, otherwise the Reviewer whose model it is); `Behaviour.seatAvailable` joins them. \"`available` carries it, and `seat` says which row of RUN-21's table each Seat reads; `Behaviour.seatAvailable` joins them\" would be exact.\n3. \"left out of its Round's `panel` members\" can be read as Panel membership, which RUN-15 says the Reviewer keeps. \"left out of its Round's `panel` entry in the trace\" removes that reading.\n\nAcceptance: the bullet still true, the three points addressed or explicitly declined in the commit message; `nix develop --command bash tools/check-all.sh` exits 0, unpiped; only that ADR changes.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T04:01:18.163241161Z","created_by":"master","updated_at":"2026-09-23T16:36:04.467753454Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-rounds-fallthrough-a5n","title":"The fake and the model disagree past a script's last Round (rounds column), and two test-panel-trace nits","description":"Follow-up from the Panels of Run 20260923T042304.628017728Z-505649 (rl-probe-shape-fallthrough-2dc,\ncommit 3166c52). Nothing is red; every item is the same family of trap that bead closed for the\nprobe column, or a nit left within that Round's scope. Land this before\n`rl-probe-block-some-class-1fx` (which grows the enumeration) and before `rl-0pj` (which\ntightens the CI control for the block this bead extends).\n\n1. The `rounds` column has the mismatch the probe column had. Past the last scripted Round,\n   `round_field` in conformance/fakes/agent returns empty, so the Implementer defaults to `ok`, the\n   Panel to `all`, and the judge arm calls `write_finished \"\"`, whose case matches nothing, so the\n   Manager writes neither file (`same`). `behaviour` in tools/formal/Rloop/Scenarios.lean says the\n   opposite for all three: `.getD false`, `.getD .none`, and a judge `{ ok := false, … }`, with the\n   docstring \"beyond the script (never reached, since the enumeration stops where the Run stops)\n   the agents fail\". Unreachable today for the same reason the probe case was — the enumeration\n   extends a script only while the Run continues — and every hand-written conformance/run item\n   scripts each Round it runs. Confirmed independently by all four Reviewers of Round 1. Fix it the\n   way the probe was fixed: pick a side (the fake failing past its script matches the model and\n   is behaviour-identical on every committed line and hand-written item), say so in the fake's\n   header and the model's docstring, and add the case to test-panel-trace's executable-free\n   checks next to the probe one, with a control that restoring the old default goes red (F5).\n   Other open beads touch the fake's header; rebase the header, not the logic.\n\n2. README.md line 52, the repository-layout row, still describes `test-panel-trace` as \"the\n   regression checks for the suite's Panel-membership comparator\"; the Gates row at line 28, the\n   file's own header and check-all.sh's label now name the fake's probe check too. One clause.\n\n3. The probe check covers one case: a one-shape script at Round 2. A fake that fell through\n   correctly for one-shape scripts but repeated the last of `a|b` at Round 3 would pass. Take or\n   leave — widening it is one more `probe_writes 3 'fable|opus'` line, not an alphabet change\n   (F4 is untouched either way).\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; the scenario count\nis unchanged by this bead and `tools/check_scenarios.py` reports nothing to write; each new\nassertion demonstrated red against a scratch copy holding the old default;\n`conformance/run /home/master/p/rloop-bash/result/bin/rloop --self-check` (github:douglaz/rloop-bash\nat 02c6206, built with nix build) exits 0 with every emitted item PASS.\n","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-23T05:07:46.265190041Z","created_by":"master","updated_at":"2026-09-25T08:00:37.326045591Z","closed_at":"2026-09-25T08:00:37.325996579Z","close_reason":"Landed in 035644f: the fake fails past its script's last Round (and for a field a group omits), matching Scenarios.lean's behaviour; the rule is stated in the fake's RLOOP_FAKE_ROUNDS header entry and the behaviour docstring; test-panel-trace checks it among the executable-free checks with an in-file F5 control (a scratch copy holding the old succeeding defaults goes red), and its probe check is widened by probe_writes 3 'fable|opus'. Gates exit 0 unpiped with the same two pre-existing advisories; check_scenarios reports nothing to write and scenarios.tsv is byte-identical at 237 scenarios. The acceptance line asking for conformance/run --self-check to exit 0 with every item PASS was stale: rloop-bash 02c6206 lags this specification and fails eleven items either way. Parity held instead: 19 passed, 11 failed with the same eleven names before and after, CNF-3 still 4 of 237, its four diagnostics now reading impl2:fail,panel2:none instead of impl2:ok,panel2:all.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-5wl","title":"check-all.sh is not safe to run concurrently in one worktree: the formal gate truncates files the regions gate reads","description":"Observed by opus on the Panel of Round 2 of Run 20260923T050941.403187119Z-505649\n(rl-agents-end-turn-on-background-check-twn, commit 0567f2b). Its first\n`nix develop --command bash tools/check-all.sh` exited 1 with\n\n  01-run-lifecycle.md:173: Rloop.Render.decisionTable is not in the index -- a region's declaration is tagged\n  FAIL  regions\n\nwhile three other Reviewers were running the same script in the same worktree (rloop's Panel is\nfour concurrent Reviewers in one tree, and PRM-4 tells each to run the build or the tests).\ntools/check_formal.sh:33 does `lake exe gate > .lake/index.jsonl`, which truncates a file that\ntools/check_regions.py:66 then reads; `.lake/index.jsonl`, `regions.jsonl` and\n`tools/formal/.lake/scenarios.tsv` are shared mutable state. `python3 tools/check_regions.py` alone\nand a clean re-run of the full gate were green, so it is a phantom red, not the change — but every\nRound's Panel will keep producing it, and a Reviewer that does not re-run reports a red gate to\nthe Manager. rl-0pj records a neighbouring mktemp/TMPDIR flake in panel-trace: same\nshared-scratch family.\n\nFix options: write each generated file to a temp name and `mv` into place (atomic on one\nfilesystem), or take a lock (`flock`) around the formal gate, or have the render/index steps\nwrite under a per-invocation directory. Cheapest that closes the observed race is write-then-mv\nfor the three files.\n\nAcceptance: four concurrent `nix develop --command bash tools/check-all.sh` in one clean\nworktree all exit 0 (demonstrated, F5: the race shown red first with the current script, for\nexample by a loop that reproduces the truncate-during-read, then green after); the single-run\ngate output unchanged; README's Gates table untouched unless a gate's behaviour changes.","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T05:36:13.855720877Z","created_by":"master","updated_at":"2026-09-23T06:09:31.827344367Z","closed_at":"2026-09-23T06:09:31.827297859Z","close_reason":"Done in a6709b4 and c777d61: check_formal.sh writes index.jsonl, regions.jsonl and scenarios.tsv to a per-process temporary and renames it into place, and the helper fails when the rename fails. Race reproduced red with the old script (2413/5606 reader runs, 30/40 staggered whole-gate runs) and green after (0); rename failure shown green before and red after with an mv stub. Round 2 Panel: no findings. Follow-up filed: nix flake check red on test-panel-trace's shebang.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-flake-check-env-shebang-ur8","title":"nix flake check is red at HEAD: check-all.sh execs test-panel-trace through a /usr/bin/env shebang the sandbox lacks","description":"Observed by the Implementer and three Reviewers of Run 20260923T053710.701098015Z-505649\n(rl-5wl, commits a6709b4 and c777d61), and confirmed by reading: `nix build\n.#checks.x86_64-linux.gates` (what `nix flake check` runs) is red at HEAD and was red at the\nbase 6a756e5, on the panel-trace gate:\n\n  conformance/test-panel-trace: /usr/bin/env: bad interpreter: No such file or directory\n\ntools/check-all.sh line 38 runs `conformance/test-panel-trace` directly, so the sandbox resolves\nits `#!/usr/bin/env bash` shebang, and the sandbox has no /usr/bin/env. Every other gate is run\nas `bash tools/…` or `python3 tools/…` and passes there; the formal gate passes there too, with\nthe atomic writes of rl-5wl (mv and wc are coreutils, which the derivation has).\n\nNothing is unguarded today: CI (.github/workflows/gates.yml) enters `nix develop` and does not\nrun `nix flake check`. But flake.nix line 32 says \"`nix flake check` runs the same gates CI\ndoes\", which has not been true for as long as test-panel-trace has existed, and the rl-5wl brief\nreasoned from a green `nix flake check` when weighing a flock dependency.\n\nFix: `bash conformance/test-panel-trace` in check-all.sh's panel-trace line (one word; the\nscript's shebang and executable bit stay for direct use), or give the derivation a /usr/bin/env.\nNeighbour of rl-0pj (same script, its mktemp and the negative controls) without being the same\nbug.\n\nAcceptance: `nix build .#checks.x86_64-linux.gates` exits 0 (it builds Lean from scratch in the\nsandbox, so allow for that); `nix develop --command bash tools/check-all.sh` exits 0, unpiped,\noutput unchanged; gates.yml untouched unless it gains a `nix flake check` step, in which case say\nwhy the duplicate run is worth its minutes.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T06:09:31.759758893Z","created_by":"master","updated_at":"2026-09-23T06:23:02.319992668Z","closed_at":"2026-09-23T06:23:02.319942965Z","close_reason":"Fixed by 902e9ae (patchShebangs in the gates derivation) and 3a3c919 (gates.yml runs nix flake check). nix build .#checks.x86_64-linux.gates exits 0 at 3a3c919 with every gate PASS; dev-shell gates unchanged and green. Follow-up nits filed separately.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-5mk","title":"gates.yml: nix flake check -L, and the README Gates section names the sandboxed run","description":"Two nits from the rl-flake-check-env-shebang-ur8 Panel (Run 20260923T061017.334217178Z-505649, commits 902e9ae and 3a3c919), neither a defect.\n\n1. The new last step of .github/workflows/gates.yml runs 'nix flake check' without -L. On a red build Nix prints only 'Last 25 log lines'; panel-trace is the last gate so today's known red would survive the cut, but an earlier gate's diagnostic would be scrolled off by check-all.sh's SUMMARY table, in a set whose own gate writes 'a line no CI log hides'. Adding -L prints the derivation's full log. Optionally '--option sandbox true' pins the property the step's comment claims, should cachix/install-nix-action ever stop leaving the Linux default on.\n\n2. README.md, Gates section ('CI runs the same script on every push'): still true, but it is now the one reader-facing place that does not say CI also builds the sandboxed derivation (flake.nix checks.gates). One clause closes it.\n\nAcceptance: gates.yml step reads 'nix flake check -L'; README's Gates section mentions the sandboxed run in a sentence; nix develop --command bash tools/check-all.sh exits 0 unpiped.","notes":"Acceptance also requires: nix flake check -L exits 0, unpiped — the step this bead changes is the sandboxed derivation, which the dev-shell gate run does not exercise. Land after rl-0pj, which rewrites the other steps of the same workflow.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-23T06:23:02.258355181Z","created_by":"master","updated_at":"2026-09-25T09:06:49.608880738Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-5mk","depends_on_id":"rl-0pj","type":"blocks","created_at":"2026-09-23T16:36:04.847785854Z","created_by":"master","metadata":"{}","thread_id":""}],"comments":[{"id":7,"issue_id":"rl-5mk","author":"master","text":"From opus on the Panel of Run 20260925T083727.810322153Z-2268035 (rl-0pj, commit 325337c), for the step this bead edits: the comment above 'Run all gates in the Nix build sandbox' (.github/workflows/gates.yml:147-151) says 'The steps above run in a nix develop shell on an Ubuntu runner and cannot show that the derivation builds'. That was already true only of the formal proof step; rl-0pj left eight proof steps above it, of which one enters nix develop. Its point still stands - only the sandboxed step shows the derivation builds - so re-word the premise rather than drop the comment.","created_at":"2026-09-25T09:06:49Z"}]}
{"id":"rl-cnf29-not-named-stream-pg8","title":"CNF-29 asserts an absence no requirement states; assert presence only (settled: reading C)","description":"`CNF-29` asserts the word `manager` appears nowhere on standard error; no requirement says so.\nDrop the absence check and assert presence only.\n\nFound by the opus Reviewer in Round 1 of the Run on `rl-seat-items-hygiene-a5t` (Run Directory\n`20260923T062347.389579851Z-505649`); the judge call of that Round was lost at its bound, so this\nwas filed by hand from the review. **Settled by the owner on 2026-09-23 after consulting fable,\nopus and astra, who agreed unanimously:** `RUN-22` states what the refusal message must carry —\n`naming the Seat — both, when both are — its model, and the reset the probe reported` — and\nnothing about what it may not carry. The set writes `and nothing else` wherever it means\nexclusivity (`RUN-7`, `RUN-10`, `RUN-11`, `DIR-4`) and `RUN-22` does not; `RUN-10` leaves every\nother line of the stream `not specified`; and `RUN-22` gives the message no layout, so \"the\nrefusal message\" has no boundary a check could use. A message that also says\n`seat manager (…) is fine` is conforming. Do not amend `RUN-22`; do not treat this as open.\n\nIt was live: `not_named` (in `conformance/run`, near line 561) greps the whole stream, so the\nsuite went red on rloop-bash's own progress lines when the same check was tried on `CNF-30`, and\n`CNF-29` passes today only because its Run is refused before any progress line carries a Seat word.\n\n## What to change\n\n- `CNF-29`: delete the `not_named` helper and both calls, and the prose sentence\n  `In neither Run does standard error name manager …`. Replace it with one sentence saying the\n  item asserts presence only, because `RUN-22` sets the message's contents and no bound on them\n  and `RUN-10` leaves the rest of the stream unspecified.\n- Keep every other assertion as it is, per Run on both rows of `RUN-21`'s table: exit 2; no\n  `manager`, `implementer` or `reviewer` argv recorded; the probe's argv recorded once; standard\n  output empty; the Run Directory holding exactly the three `probe-pick.*` files;\n  `probe-pick.md` reading `manager:unknown` and `implementer:unavailable` with the family lines;\n  and, over the whole of standard error, the word `implementer` standing whole once every\n  `manager-model` and `implementer-model` (with any dashes before it) is deleted, as `CNF-28`'s\n  prose defines, the configured model name, and the probe's matching line verbatim.\n- Fold in one unrelated suggestion from the same review: `conformance/run`'s `CNF-23` block has\n  the same `tr '\\0' '\\n' < \"$(ls … | head -1)\"` trap the Round closed for `CNF-24` — when no argv\n  matches, bash prints `: No such file or directory` on the suite's stderr. Guard it the same way.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; use a scratch copy of its `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- `CNF-29` still goes red against each of: a scratch copy that refuses only for the Manager's\n  Seat (the pick runs); one that hardcodes `fable` for the Implementer's Seat (Run 2 not refused);\n  one whose message names the flag and no Seat (`implementer` does not stand whole); one that\n  omits the probe line or the model. Demonstrate each (`F5`).\n- `CNF-29` is green against a scratch copy whose progress lines name Seats, and against one whose\n  refusal message also says the other Seat is fine. State in the prose that the mutant no longer\n  caught — a message naming both Seats as unavailable when one is — is forbidden by no sentence\n  in the set, and that the per-Seat reading stays asserted where it is observable,\n  `probe-pick.md`.\n- Against a scratch copy that omits the codex judge record, `CNF-23` fails with its own\n  diagnostic and the suite prints no shell `No such file or directory` line.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T09:22:15.221201693Z","created_by":"master","updated_at":"2026-09-25T03:17:26.134364992Z","closed_at":"2026-09-25T03:17:26.134310600Z","close_reason":"Closed by hand 2026-09-25 after Run 20260925T024030.524416029Z-367594 ended on RUN-12: the judge ran the suite through its shell tool's background facility and ended its turn (F16's judge twin; PRM-2 lacks the foreground sentence PRM-3/PRM-4 gained). The Round's work stands as the judge had already accepted it: four Reviewers, no findings; opus's two-word wording fix applied; gates 8/8; rl-csw (filed by the judge) holds the remaining nits.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-seq7-failed-run-stdout-dof","title":"A Run of a Sequence that ended 2 still prints its Finished File (SEQ-7 untested)","description":"A Run that ended 2 with a Finished File on disk still contributes to standard output, in a\nSequence and in a lone Run. The suite does not check either.\n\nFound 2026-09-23 by a three-model review of rloop-bash `02c6206` (opus, astra and sol\nindependently). `SEQ-7` says `A Run that ended 2 prints nothing, as RUN-10 says of a lone one`.\nThree rows of `RUN-11`'s table reach exit 2 *with* `finished.md` on disk — a first line that is\nnot a status, `STATUS: idle` after a Round, a judge that wrote `STATUS: done` then exited\nnon-zero — and `DIR-8`'s checkpoint failures do too. rloop-bash prints the block before it looks\nat the status and the suite is green; observed:\n\n```\n$ RLOOP_FAKE_RUNS=done,other rloop --auto        # Run 2's pick writes \"STATUS: maybe\"\nexit=2\n== run 1 ==\nSTATUS: done\n…\n== run 2 ==\nSTATUS: maybe\n```\n\nWhy the suite is green: `CNF-20`'s \"a pick that fails\" row and `CNF-14`'s only exit-2 row\n(`conformance/run:272`, `RLOOP_FAKE_PICK=fail`) both use a failing pick, which leaves no file to\nprint. Neither item can tell a correct Implementation from one that prints whatever it finds.\n(rloop-bash happens to get the lone path right, `bin/rloop:256`; the item does not show it.)\n\n## What to change\n\n- Extend `CNF-20` (or add an item beside `CNF-21`) with a Sequence whose Run 1 ends 0 and whose\n  Run 2 ends 2 with a Finished File on disk, in at least two shapes: a pick whose first line is\n  not a status, and a Round whose judge writes `STATUS: idle`. Assert standard output is\n  byte-identical to Run 1's block alone and the exit is 2.\n- Add the same two shapes to `CNF-14` for the lone Run: exit 2, standard output empty,\n  `finished.md` present.\n- `README.md`'s exit-2 bullet says `After a Run that exited 2 there is no Finished File`, which\n  `RUN-11`'s table contradicts. Amend it: a Run that exited 2 may still hold a Finished File (an\n  invalid status, or one written by a Manager call that then failed); `RUN-10` keeps it off\n  standard output; inspect the kept Run Directory.\n- Both shapes are scriptable with the fake's existing `RLOOP_FAKE_RUNS` knob (`done,other`; and\n  `done,task` with `RLOOP_FAKE_ROUNDS=\"impl=ok;panel=all;judge=idle\"`). Extend\n  `conformance/fakes/agent` only if a shape cannot be scripted, and document any new knob in its\n  header.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; `nix build` there and use `./result/bin/rloop`, or a\nscratch copy of `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- The new Sequence rows fail against rloop-bash `02c6206`; demonstrate the red on a scratch copy\n  (`F5`). The lone-Run row is green against it and red against a scratch copy whose lone-Run\n  branch drops the `!= 2` test; demonstrate that too.\n- The coverage gate still cites `SEQ-7` and `RUN-10` from items.\n\n## Not in scope\n\nChanging `SEQ-7` or `RUN-10`.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.262632856Z","created_by":"master","updated_at":"2026-09-24T16:21:31.410271149Z","closed_at":"2026-09-24T16:21:31.410217929Z","close_reason":"Landed as 7dcf842 (CNF-14 and CNF-20 rows for a Run that ended 2 with a Finished File on disk) and 241bad1 (README exit-2 bullet). Demonstrated on /tmp/rl-seq7/u0 (CNF-20 red), u1 (CNF-14 red) and u2 (both green); u2's only red is CNF-5, rloop-bash 02c6206 predating 8d3d5a8. Panel nits filed as rl-8nl.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-prm6-instruction-literal-gnt","title":"Placeholders inside INSTRUCTION are substituted (PRM-6 untested)","description":"Placeholder text inside the caller's `INSTRUCTION` must survive rendering literally. Neither the\nsuite nor its own renderer honours that today.\n\nFound 2026-09-23 by a three-model review of rloop-bash `02c6206` (opus, astra and sol\nindependently). `PRM-6` requires `literal substitution of each {{NAME}} below with its value and no\nother change`. An Implementation that substitutes placeholders in sequence, `{{INSTRUCTION}}`\nfirst, then runs the remaining passes over the instruction's own text. Observed:\n\n```\n$ rloop 'Repair literal {{UNAVAILABLE}} and {{ROUND}} tokens'\n# the Manager received:  Repair literal  and 0 tokens\n```\n\n**The suite cannot catch this as it stands.** `conformance/run`'s own `render()` (near line 139)\nsubstitutes `{{INSTRUCTION}}` first and re-scans the result, byte for byte what rloop-bash does\nat `bin/rloop:243`, so the expected prompt cascades identically and a new row would stay green.\nUnder bash 5.2+ `patsub_replacement` an unquoted replacement also turns `&` into the matched\ntext, on both sides. Fix the renderer first.\n\n## What to change\n\n1. **Amend `PRM-6`**, the rule's home: the render is one pass — a value substituted in is never\n   re-scanned, so placeholder text inside a value survives literally. Add a dated italic note\n   naming the cascade that motivated it. `CNF-12`'s prose cites `PRM-6`; it does not restate the\n   rule (`AGENTS.md`: one rule, one home).\n2. **Fix `conformance/run`'s `render()`** so no value is rescanned: render in one pass and quote\n   every replacement (`\"${P_X}\"`). \"Substitute `{{INSTRUCTION}}` last\" is not enough — a Run\n   Directory path holding `{{INSTRUCTION}}` (given with `--run-dir`) would then have the\n   instruction substituted into it. Demonstrate that the fixed renderer, given an instruction\n   holding `{{TASK_FILE}}`, `&` and `\\` and a `--run-dir` path holding `{{INSTRUCTION}}`, produces\n   both values' bytes unchanged.\n3. **Add one Run to `CNF-12`** whose instruction contains placeholder names and the\n   substitution-hostile bytes — at least `{{TASK_FILE}}`, `{{UNAVAILABLE}}`, a backslash, `&` and\n   `%s` — and whose `--run-dir` path contains `{{INSTRUCTION}}` (the cyclic case), and assert the\n   recorded pick prompt equals the fixture rendered with every value's bytes unchanged. Mind the block's structure: `prompts_ok` is snapshotted before the fourth\n   Run (`conformance/run` near line 226), so a Run placed after the snapshot feeds no verdict.\n   Put the new Run before it or include its result; `rl-aa4` touches the same lines and can be\n   rebased either way.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; use a scratch copy of its `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- With the fixed renderer, the row fails against rloop-bash `02c6206`, fails against a scratch\n  copy that merely substitutes `{{INSTRUCTION}}` last (the cyclic case catches it), and passes\n  against a one-pass scratch copy; demonstrate all three (`F5`).\n- Corrupting only the new Run's recorded prompt turns `CNF-12` red (the verdict sees it).\n- Every other prompt row is unchanged: the four fixtures are byte-identical to before.\n\n## Not in scope\n\nEscaping rules for the instruction — there are none, and none are wanted. `PRM-1`–`PRM-5`'s\ntext.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.319812484Z","created_by":"master","updated_at":"2026-09-24T16:54:59.383459520Z","closed_at":"2026-09-24T16:54:59.383410347Z","close_reason":"Done in 9e77a81 and 70648f5: PRM-6 states the render is one pass with a dated note; conformance/run's render() walks the template once and never rescans a value; a hostile Run (instruction with {{TASK_FILE}}, {{UNAVAILABLE}}, {{ROUND}}, backslash, & and %s; --run-dir holding {{INSTRUCTION}}) joins CNF-12 before the prompts_ok snapshot and its argv joins CNF-11. Demonstrated on scratch copies of rloop-bash 02c6206: cascade red, INSTRUCTION-last red, one-pass green, backslash-dropped red; CNF-5 and CNF-20 were already red there. Four Reviewers, no findings; their hygiene nits went to rl-aa4. rloop-bash needs the one-pass renderer as its next instruction.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-agt2-usage-corners-oor","title":"AGT-2 usage-error corners CNF-5 does not exercise: empty --run-dir, swallowed values, --=x, --auto=1, octal","description":"`AGT-2`'s usage errors have corners `CNF-5` does not exercise, and rloop-bash spawns agents in\nfour of them.\n\nFound 2026-09-23 by a three-model review of rloop-bash `02c6206` (astra, sol, opus). Depends on\n`rl-zero-durations-9uy`, which amends the same sentence of `AGT-2` and the same `CNF-5` block:\nland after it and rebase on its wording.\n\n| invocation | what happened | rule |\n|---|---|---|\n| `--auto --run-dir ''` | Sequence ran: the check tests the value's emptiness, not the option's presence | `SEQ-3` |\n| `--implementer-model --max-rounds 1` | the missing value swallowed `--max-rounds`; the Implementer got model `--max-rounds` | `AGT-2` missing value |\n| `--=x` | rewritten to `--` `x`, which an option terminator accepted | `AGT-2` unknown option |\n| `--auto=1`, `--help=x`, `--version=x` | a value given to a flag that takes none was accepted; `--auto=1` ran a Sequence with instruction `1` | no rule today — see below |\n| `--auto --max-runs 08` | passed validation as numeric, then failed as Bash octal (`value too great for base`), exit 2, nothing spawned | `AGT-2` says numeric and not decimal |\n\n`AGT-1` says only `Long options MAY also be given as --flag=value`; it does not restrict the form\nto options that take a value, and `AGT-2` lists no error for `--auto=1`. A `CNF-5` row may not\nassert what no requirement states (the principle `rl-cnf29-not-named-stream-pg8` is filed on),\nso `AGT-2` gains the rule first.\n\n## What to change\n\n**Amend `AGT-1`'s sentence** to `A long option that takes a value MAY also be given as --flag=value`, and **amend `AGT-2`** in three sentences: a value is missing when the option is last or when the next\nargument begins with `--`, so a flag never becomes another flag's value; a number is a string of\ndecimal digits read in base ten, so `08` is `8`; a value given with `=` to `--auto`, `--help` or\n`--version`, which take none, is an unknown option.\n\n**Extend `CNF-5`** with the usage-error rows above (`--auto --run-dir ''`, the swallowed value,\n`--=x`, `--auto=1`, `--help=x`, `--version=x`): each exits 2, spawns nothing, writes to standard\nerror. `conformance/run`'s `CNF-5` loop word-splits an unquoted `$args`, so `--run-dir ''` cannot\npass through it (it arrives as the two-character argument `''` and the row goes green for\n`SEQ-3`'s ordinary reason); drive the new rows from a bash array so an empty argument survives.\n\n**Add two positive controls** as their own sentence after `CNF-5`'s `--help`/`--version` one,\nsince `CNF-5` opens with `exits 2, spawns nothing`: `--auto --max-runs 08` with a Manager\nscripted idle spawns one pick and exits 0 (`SEQ-6`: an idle Run ends the Sequence with 0), which\nshows `08` was read as eight rather than rejected; and `--auto --max-runs 08` with nine `done`\nRuns scripted stops after exactly eight picks and exits 2 (`SEQ-5`), which shows the bound was\nhonoured, not ignored. rloop-bash `02c6206` already exits 2 on `08` for the wrong reason, so the\nfirst control is the red against it.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; use a scratch copy of its `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- Every new usage-error row and the first positive control fail against rloop-bash `02c6206`;\n  the second control fails against a scratch copy that ignores `--max-runs`; demonstrate each\n  (`F5`).\n- Argv fixtures untouched (no command line changes); `tools/check_fixtures.py` reports nothing to\n  write.\n\n## Not in scope\n\nValidating model names. Short options. Defining a `--` terminator: `AGT-1` has none, so `--=x` is\nan unknown option and that is all this bead says about it.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.358800803Z","created_by":"master","updated_at":"2026-09-24T16:01:36.498186470Z","closed_at":"2026-09-24T16:01:36.498136195Z","close_reason":"Landed as 3e57039 (AGT-1 restricted to options that take a value; AGT-2 gains the missing-value, base-ten and = rules with a dated note; the two Panel nits folded in) and 8d3d5a8 (CNF-5: six new rows through one usage_error call per row so the empty argument survives, two --max-runs 08 controls citing SEQ-5 and SEQ-6). F5 evidence: rloop-bash 02c6206 (u0) fails CNF-5 alone, its detail naming the six rows, both controls and the three zero-duration rows from bf4730c that 02c6206 also predates; a scratch fix (u1) is 30/30; a copy of u1 that ignores --max-runs (u2) fails CNF-5 on the second control and, inherently, CNF-20's existing --max-runs 3 row. Panel: fable, opus, astra pass; sol's two blockers were the brief's mispredicted mutant output, not defects. Nits filed as a follow-up bead.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0,"dependencies":[{"issue_id":"rl-agt2-usage-corners-oor","depends_on_id":"rl-zero-durations-9uy","type":"blocks","created_at":"2026-09-23T16:36:04.719384891Z","created_by":"master","metadata":"{}","thread_id":""}],"comments":[{"id":5,"issue_id":"rl-agt2-usage-corners-oor","author":"master","text":"rl-zero-durations-9uy landed as bf4730c; rebase on its AGT-2 sentence ('a number below 1 for --max-rounds, --max-runs, a timeout or --kill-after'). Two cosmetic nits from its Panel (fable, opus) to fold in while touching the same lines: (1) 06-conformance.md's CNF-5 paragraph ends on a 148-column line, reflow it to about 100 when the new rows go in; (2) the 2026-09-24 note in AGT-2 says 'until then a timeout could be 0', which is narrower than the rule it dates: say 'a timeout or --kill-after'. Opus also suggested the note name the Probe's bound (AGT-14: 'and the Probe's (AGT-18) by --probe-timeout'); take or leave.","created_at":"2026-09-24T15:26:16Z"}]}
{"id":"rl-zero-durations-9uy","title":"A duration of 0 is accepted by AGT-2 and unbounded under AGT-14; make it a usage error","description":"`AGT-2` permits a duration of zero and `AGT-14` requires every call to be bounded; GNU `timeout`\nreads zero as unbounded, so a conforming-looking Implementation has no bound at all.\n\nFound 2026-09-23 by a three-model review of rloop-bash `02c6206` (astra and sol independently).\n`AGT-2` rejects a timeout `below 0`, so `0` is accepted; `AGT-14` says every agent call and the\nProbe `MUST be bounded by its role's timeout`. Observed: `--probe-timeout 0` let a sleeping Probe\nrun past any bound; `--implementer-timeout 1 --kill-after 0` with a SIGTERM-ignoring fake never\nsent SIGKILL. `timeout --help`: `a duration of 0 disables the associated timeout`.\n\n**Settled here, so the Run does not block on it: zero is a usage error.** Every duration —\nthe four timeouts and `--kill-after` — must be at least 1; `AGT-2`'s `below 0 for a timeout`\nbecomes `below 1 for a timeout or for --kill-after`. Reasoning: a zero bound is never what a\ncaller means, `--kill-after 0` as \"SIGKILL at once\" is a feature nobody asked for, and one rule\nfor all five durations is simpler than a special case. The owner may reverse this; if so the\nchange is the opposite rows, not a different bead.\n\n## What to change\n\n- Amend `AGT-2` as above, with a dated italic note naming the `timeout` behaviour that motivated\n  it.\n- Extend `CNF-5` with `--probe-timeout 0`, `--kill-after 0` and one of the other timeouts at `0`:\n  exit 2, nothing spawned, standard error written.\n- `AGT-1`'s usage block does not state ranges; leave it.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- The new rows fail against rloop-bash `02c6206`; demonstrate (`F5`).\n- No scenario or fixture changes; the tools report nothing to write.\n\n## Not in scope\n\nFractional durations. Changing any default.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.398200546Z","created_by":"master","updated_at":"2026-09-24T15:26:08.836177232Z","closed_at":"2026-09-24T15:26:08.836124164Z","close_reason":"Done in bf4730c: AGT-2 rejects a number below 1 for the timeouts and --kill-after with a dated note quoting GNU timeout; CNF-5 gains --reviewer-timeout 0, --probe-timeout 0, --kill-after 0; shown red on rloop-bash 02c6206 (9 spawns each) and green on a five-line scratch fix; all gates PASS, four Reviewers no findings.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-exact-bytes-nul-gitignore-pjc","title":"Byte-for-byte rules the suite meets by eye: NUL in RUN-9 and RUN-21 lines; DIR-2 gains the .gitignore byte rule","description":"Three rules an Implementation can meet by eye and fail by bytes, none exercised by the suite.\nTwo already say bytes; `DIR-2` does not and gains the rule here.\n\nFound 2026-09-23 by a three-model review of rloop-bash `02c6206` (astra and sol independently):\n\n1. **`RUN-9`**: the Finished File's first line is matched `byte for byte` (`compared exactly: no\n   trimming, no case folding, no CRLF tolerance`), but Bash `read` drops NUL bytes, so\n   `STATUS: do\\0ne` is accepted as `done`\n   (`raw_first_line_bytes=13 bash_value_bytes=12 verdict=done`).\n2. **`RUN-21`**: the same `read` in the Probe's parser, so `Current week (Fable): 1\\0`\n   `00% used` — bytes that do not contain the anchored prefix — still marked the Seat\n   `unavailable` and the refusal message quoted altered bytes. Fail-open requires `unknown`.\n3. **`DIR-2`**: says `.rloop/.gitignore` must exist `containing the single line *` and no more.\n   It names no bytes and does not say what happens to a pre-existing file holding `*\\n\\n` or `*`\n   without a newline; rloop-bash checks it through command substitution, which loses trailing\n   newlines, and accepts both unchanged. `CNF-8`'s own check (`conformance/run` near line 97,\n   `[ \"$(cat …)\" = \"*\" ]`) has the same trap, so no row could go red today.\n\n**Settled here, so the Run does not block on it: `DIR-2` gets the byte rule.** The set's own\nconvention decides it: `RUN-21` says `Every line of either record ends with a newline, the last\none included`, and `RUN-15`'s newline question was settled the same way on 2026-09-22 (`rl-vcq`).\n\n## What to change\n\n- Amend `DIR-2`: the file holds exactly the two bytes `*` and a newline, and rloop rewrites one\n  whose bytes differ. Dated italic note naming the two accepted-by-eye shapes.\n- A `CNF-14` row where the Manager fake writes a first line with a NUL byte inside\n  `STATUS: done`: the Run ends 2 (not a status), standard output empty.\n- A `CNF-26` shape (or a new shape in the fake's probe script) whose matching line carries a NUL\n  byte inside the percentage: every Seat `unknown`, the Run proceeds.\n- `CNF-8`: replace the check with a byte comparison (`printf '*\\n' | cmp -s - …`), then add two\n  rows where the file pre-exists as `*\\n\\n` and as `*` without a newline: after the Run it is\n  exactly `*\\n`.\n- The fake needs to emit a NUL; `printf '\\0'` in `conformance/fakes/agent` will do. Document the\n  knob in its header. Other open beads touch that header (`rl-timeout-descendants-qqh`,\n  `rl-rounds-fallthrough-a5n`, `rl-probe-block-some-class-1fx`); rebase the header, not the logic.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; use a scratch copy of its `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- Each new row fails against rloop-bash `02c6206`; demonstrate each (`F5`).\n- `RUN-9`'s and `RUN-21`'s wording is unchanged; `DIR-2` gains the byte rule above and nothing\n  else.\n\n## Not in scope\n\nAny other encoding or line-ending rule. A general NUL policy for prompts or Feedback Files.\n","status":"closed","priority":3,"issue_type":"task","created_at":"2026-09-23T16:12:58.437583378Z","created_by":"master","updated_at":"2026-09-24T22:31:49.552695574Z","closed_at":"2026-09-24T22:31:49.552646591Z","close_reason":"Done in fd1d87c: DIR-2 byte rule, CNF-8 cmp plus two pre-existing shapes, CNF-14 ok:nul row, CNF-26 nul shape; each red against rloop-bash 02c6206 and green against a fixed copy; four Reviewers, no accepted findings","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-timeout-descendants-qqh","title":"A timed-out agent's SIGTERM-ignoring descendant outlives rloop's exit (AGT-15 untested)","description":"A timed-out agent's descendant that ignores SIGTERM can outlive rloop's exit; `AGT-15` forbids it\nand no item forces the `--kill-after` SIGKILL.\n\nFound 2026-09-23 by the astra reviewer on rloop-bash `02c6206`. `AGT-14` sends SIGTERM to the\ncall's process group, waits `--kill-after`, then SIGKILLs the group. `AGT-15`: `rloop MUST NOT\nexit while any agent process it started … is running`. rloop-bash's reaper waits for the\n`timeout` wrapper only, then abandons the group; with a Manager fake that spawned a\nSIGTERM-ignoring child and `--manager-timeout 1 --kill-after 1`, rloop exited 2 at 1.04 s with the\nchild still alive (`child state when rloop exited: S`).\n\nWhat the suite has: `CNF-17` already checks a descendant — `the fake's process and the grandchild\nit spawned are gone before the judge starts`, spawned with `RLOOP_FAKE_GRANDCHILD=1` — but that\ngrandchild is a plain `sleep 300 &`, which the group's SIGTERM kills, so nothing makes rloop wait\nout `--kill-after`. `CNF-18`'s prose claims `no process of the fake's group survives` while\n`conformance/run` (near line 324) checks only the fake's own pid and sets no grandchild.\n\n## What to change\n\n- Extend `RLOOP_FAKE_GRANDCHILD` so the spawned child traps and ignores SIGTERM\n  (`trap '' TERM; sleep 30`) when a second knob is set; document the combination in the fake's\n  header. Do not add a second pid-recording mechanism.\n- Add a row to `CNF-17` with that child: after the timed-out Reviewer's call, the child is gone\n  before the judge starts. Add a Manager-timeout row (a sleeping pick under `--manager-timeout 1\n  --kill-after 1`, the shape observed): the fake's pid and the recorded descendant's are both\n  gone before the executable returns 2. Add the grandchild to `CNF-18`'s SIGINT path so its\n  prose is true, with the same two-pid assertion.\n- \"Gone\" means not running: after rloop has exited, every ancestor of the child is dead, so it\n  cannot stay a zombie and `kill -0` failing is sufficient; if a Reviewer shows otherwise, read\n  the state from `/proc/<pid>/stat` and say so in the item.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; use a scratch copy of its `bin/rloop` under `/tmp`.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- The new rows fail against rloop-bash `02c6206`; demonstrate (`F5`), and confirm a scratch copy\n  that waits for the whole group passes.\n- `CNF-17`'s own timing bound still holds (`conformance/run` asserts the Implementer is gone\n  within ten seconds); a correct wait costs at most `--kill-after`.\n\n## Not in scope\n\nProcesses that escape the group (`setsid`); `AGT-14` is about the group and says so.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.477250945Z","created_by":"master","updated_at":"2026-09-24T17:39:19.061975260Z","closed_at":"2026-09-24T17:39:19.061923452Z","close_reason":"Done in de6ffe9: CNF-17 proves a timed-out call's SIGTERM-ignoring child is gone before the judge starts and before exit; CNF-18 checks the child too. Panel: four Reviewers, no findings that fail the brief; nits filed as rl-cnf17-panel-nits-evy, the AGT-15 normal-exit question as rl-agt15-normal-exit-stragglers-e6g.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-suite-restricted-path-42n","title":"The suite runs on the host PATH, so an undeclared dependency (rloop-bash's cmp) is invisible; CNF-1's own list is wrong","description":"The suite runs the executable on the host's full `PATH`, so an undeclared runtime dependency is\ninvisible; rloop-bash ships without `cmp` and stays green. `CNF-1`'s own dependency sentence is\nalso false.\n\nFound 2026-09-23 by the opus reviewer on rloop-bash `02c6206`. Its flake wraps `git`, coreutils\nand util-linux, but `bin/rloop` decides `RUN-12`'s and `DIR-6`'s byte-identity with `cmp`, which is\ndiffutils. Under `nix run` on a host without diffutils, `cmp` exits 127, read as \"the files\ndiffer\": the no-decision guard can never fire (`Rloop.no_decision_off_burns`, the witness\n`RUN-12` names — five Implementers at a cap of five where one was required) and every Round's\n`task.md` is set aside as interference (`DIR-6`). Reproduced with `PATH` cut to git + coreutils +\nbash. CI never sees it because `writeShellApplication` appends to the caller's `PATH`.\n\nThis is a suite property, not an rloop-bash one: `CNF-1` promises the items are the definition of\nconformance, and a dependency the Implementation forgot to declare is a conformance failure the\nsuite cannot currently produce.\n\nTwo things stand in the way and are part of this bead:\n\n- `CNF-1` says the suite needs `bash, git, GNU coreutils, grep, sed and awk on PATH and nothing\n  else`. It also uses `cmp` (diffutils; `conformance/run` in several places and\n  `conformance/test-panel-trace`) and `find` (findutils; `conformance/run` near lines 86–88).\n- \"The directories of `sh`, `git` and coreutils\" is not a restriction anywhere those share a\n  directory (`/run/current-system/sw/bin` here, `/usr/bin` on an FHS host): `cmp` would come\n  along. The restriction must be a private directory of symlinks to named tools.\n\n**Settled here: the restriction applies to every executable invocation the suite makes**, not to\none item.\n\n## What to change\n\n1. Amend `CNF-1`'s dependency sentence to name `cmp` (diffutils) and `find` (findutils).\n2. Amend `CNF-2`, which owns `PATH`: before running the executable the suite resolves the tools\n   `CNF-1` names to absolute paths, builds a private directory of symlinks to exactly those\n   names plus what the fakes need (`bash`, `grep`, `sed`, `awk`, `tr`, coreutils), and runs the\n   executable with `PATH` = the fakes directory, then that directory, and nothing else. `cmp` and\n   `find` stay available to the suite's own code by absolute path and are not in the executable's\n   `PATH`. An executable that relies on anything more must carry it (a wrapper that prepends its\n   own inputs, as a nix `writeShellApplication` does, satisfies this). Say why in an italic note,\n   citing `RUN-12` and this incident.\n3. `conformance/test-panel-trace`'s executable checks run the same way.\n\nThe Implementation used for `F5` demonstrations is `/home/master/p/rloop-bash`\n(github:douglaz/rloop-bash) at `02c6206`; `nix build` there and use `./result/bin/rloop`, since\nthe wrapper's own `PATH` prefix is what this bead tests.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS.\n- Under the restricted `PATH`, rloop-bash `02c6206`'s `result/bin/rloop` goes red on `RUN-12`'s\n  item and on `CNF-9` (via `rejected-*` files), and a scratch build whose flake adds\n  `pkgs.diffutils` passes every item; demonstrate both (`F5`).\n- The suite itself still runs on this machine under the restriction. The spec's CI is unaffected:\n  it runs `conformance/test-panel-trace` without an executable and never `conformance/run`.\n\n## Not in scope\n\nFixing rloop-bash's flake — that is its own repository's work, to be given to its next Run as an\ninstruction. Auditing other tools `bin/rloop` calls (`uuidgen` is util-linux and wrapped; `flock`,\n`timeout`, `env` are coreutils or util-linux).\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-23T16:12:58.516000294Z","created_by":"master","updated_at":"2026-09-24T19:05:47.488685855Z","closed_at":"2026-09-24T19:05:47.488638906Z","close_reason":"done","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-6fh","title":"CNF-5 nits from the rl-agt2-usage-corners-oor Panel: a fresh repo before the --max-runs 08 controls; the '' row's prose rendering","description":"Two take-or-leave suggestions from the opus reviewer on 8d3d5a8, neither a defect.\n\n1. conformance/run's CNF-5 block runs the two `--auto --max-runs 08` controls in the same `$repo` as the seventeen usage-error rows. A green Implementation spawns nothing on those rows, so the repository is untouched and no false green is possible; on an already-red Implementation the controls' picks land in a repository earlier rows may have committed into, which muddies the diagnostic. A `repo=\"$(new_repo)\"` before the first control costs one line.\n\n2. 06-conformance.md's CNF-5 row `--auto --run-dir ''` renders two literal quote characters inside backticks, the very confusion the suite's own comment warns about; the gloss 'the value empty' disambiguates. A phrasing such as '`--auto --run-dir` with an empty value' would not need the gloss.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; conformance/run against a 30/30 scratch Implementation stays 30/30.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T16:01:36.438778982Z","created_by":"master","updated_at":"2026-09-24T16:01:36.438778982Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-8nl","title":"CNF-14, CNF-20 and README nits from the rl-seq7-failed-run-stdout-dof Panel: the STATUS: idle shape in the README bullet, stderr and finished.md in the items' prose, an empty-run1 guard","description":"Four take-or-leave suggestions from the Panel on 7dcf842 and 241bad1, none a defect.\n\n1. README.md's exit-2 bullet names the two Finished-File shapes as `a file whose first line is\nnot a status, or one a Manager call wrote and then failed`; a reader holding a finished.md that\nliterally begins `STATUS: idle` after a Round will not recognise it. Four words — \"including\n`STATUS: idle` after a Round\" — would close the gap; RUN-9 classes that case and RUN-11's table\nhas its row (opus).\n\n2. 06-conformance.md's new CNF-20 sentence names exit, Run Directory count and standard output\nbut not the non-empty standard error the block asserts (conformance/run, the `cmp -s … && [ -s\n\"$rec/stderr\" ]` line); CNF-14's sentence names it. The same for CNF-14: the block asserts\nfinished.md is present afterwards and the prose only implies it (opus, fable).\n\n3. conformance/run's CNF-20 Sequence loop: when a Sequence leaves no Run Directories, `run1` is\nempty and `cat \"$run1/finished.md\"` reads `/finished.md`, spilling noise on the suite's own\nstderr, and `echo \"$dirs\" | wc -l` reports `1 dirs`. The verdict is still fail; a `[ -n \"$run1\" ]`\nguard keeps the failure clean (fable, opus).\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; the CNF-14 and\nCNF-20 rows keep their verdicts on /tmp/rl-seq7/u0 (CNF-20 red), u1 (CNF-14 and CNF-20 red) and\nu2 (both green), or on fresh copies built the same way from rloop-bash 02c6206's bin/rloop.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T16:20:44.763620818Z","created_by":"master","updated_at":"2026-09-24T16:20:44.763620818Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf17-panel-nits-evy","title":"CNF-17 nits from the rl-timeout-descendants-qqh Panel: the 5-second Implementer claim no row asserts; the liveness snapshot after the stdin probe; the slow Reviewer's own PID; RLOOP_FAKE_GRANDCHILD driven by no row","description":"Nits from the rl-timeout-descendants-qqh Panel (commit `de6ffe9`), none of which fails the item; one\nis a prose claim no row asserts.\n\n- `CNF-17`'s Implementer sentence says `the Implementer is gone within 5 seconds` while the row\n  (`conformance/run`, the `--implementer-timeout 1 --kill-after 1` row) asserts the whole Run's\n  wall clock is under 10 seconds and never observes the Implementer process. Pre-existing; raised\n  by fable, opus and sol. Say what the row measures (\"the Run reaches the Panel within 10 seconds\")\n  or tighten the row; the prose must be what a row asserts.\n- The fake's `GRANDCHILDREN_ALIVE` snapshot is written after the stdin probe (`timeout 1 head -c1`),\n  so on an Implementation that leaves stdin open it reads \"within a second of the call's start\".\n  `CNF-10` reds such an Implementation anyway; moving the three lines above the probe costs\n  nothing (opus).\n- `CNF-17`'s Reviewer row no longer observes the slow Reviewer's own `PID`; the old prose claimed\n  `the fake's process and the grandchild` and asserted neither. One post-Run `kill -0` on\n  `*-reviewer-1-fable.env`'s `PID` restores the claim (opus, astra's Consultation answer).\n- `RLOOP_FAKE_GRANDCHILD=1` (the plain `sleep 300` sleeper in every fake) is now driven by no row:\n  only the fake's header and `kill_recorded`'s `sed` name it. Keep it for the AGT-15 straggler\n  bead (rl-agt15-normal-exit-stragglers-e6g) or drop it; a documented knob no item drives rots.\n- The suite's wall time grew about 5 s (60.6 s to about 66 s against the same executable): one\n  extra `sed` fork per fake call for the liveness line. Worth stating; not worth optimising.\n- `kill_recorded` SIGKILLs every recorded `PID`, including fakes that exited seconds earlier, so\n  a recycled pid could be hit. Theoretical (pid space in the millions); the old Implementer row\n  did the same.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T17:36:44.146160684Z","created_by":"master","updated_at":"2026-09-24T17:36:51.219826281Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-agt15-normal-exit-stragglers-e6g","title":"AGT-15 on a normal exit: does rloop wait for stragglers an agent leaves in its group? (specification question)","description":"`AGT-15`: `rloop MUST NOT exit while any agent process it started, or the Probe (AGT-18), is\nrunning`. `AGT-16` puts each agent in its own group `so that AGT-14 and AGT-15 reach every\ndescendant`. `AGT-14` says what happens at a timeout: SIGTERM the group, wait `--kill-after`,\nSIGKILL. Nothing says what rloop does with a process an agent leaves running in its group when the\nagent itself exits normally: a tool subprocess the CLI forgot, or the suite's `RLOOP_FAKE_GRANDCHILD=1`\nsleeper (`sleep 300 &` behind a fake that exits 0).\n\nTwo readings of `AGT-15`'s first sentence:\n\n- A. \"agent process\" is the process rloop spawned; when it has exited, the call is over and its\n  stragglers are nobody's. rloop-bash `02c6206` does this (`reap()` waits for the `timeout`\n  wrapper alone), and every item is green on it for this shape.\n- B. the group is the agent (per `AGT-16`'s rationale), so rloop must, after a normal exit, wait\n  for or SIGTERM-then-SIGKILL the rest of the group before going on — or at least before exiting.\n\nNoticed while briefing rl-timeout-descendants-qqh (2026-09-24): the old `CNF-17` row set\n`RLOOP_FAKE_GRANDCHILD=1` Run-wide, so the pick's and the Implementer's sleepers outlived rloop\nunder rloop-bash, and no item noticed. The specification owner should pick a reading; if B, amend\n`AGT-15` (or `AGT-14`) to say when the group is reaped after a normal exit, and give `CNF-17` a\nrow: a healthy fake that leaves a sleeper, both pids gone once the executable has returned.\nNot a Manager's call (`RUN-19`).\n","status":"open","priority":3,"issue_type":"task","created_at":"2026-09-24T17:36:44.201280897Z","created_by":"master","updated_at":"2026-09-24T17:36:44.201280897Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf2-restrict-path-skip-check-sqo","title":"restrict_path's skip and refusal paths (CNF-2) have no check the repository runs; this host skips nothing","description":"`restrict_path` in `conformance/scenario-lib.sh` (CNF-2) has two paths no check the repository\nruns can reach on this host: the skip path (a name in the GNU coreutils list the host lacks is left\nout and named once on standard error, exit 0) and the refusal path (a name the fakes need, or `cmp`\nor `find`, missing: exit 2 before any item). This host resolves every listed name, so the skip line\nnever prints in the gate run or in either conformance run; the Round 2 Reviewers exercised both\npaths by hand on a synthetic `PATH` and they behave as `CNF-2` says.\n\nWorth a check with the suite's own dependencies only, in `conformance/test-panel-trace` or a\nsibling: source the library with a thin `PATH` built from symlinks (drop three unneeded names, then\ndrop `tr`, then drop `cmp`) and assert the stderr line, the exit statuses and the messages. The\nmessages themselves say \"not on PATH\" for a failure that can also be a failed `ln` (opus, Round 2\nof the rloop Run that landed the restriction); decide whether they should say \"could not be linked\".\n\nFound by the opus Reviewer, Round 2 of rl-suite-restricted-path-42n.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T18:41:54.673184183Z","created_by":"master","updated_at":"2026-09-24T18:41:54.673184183Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf2-restrict-path-nits-50m","title":"restrict_path nits from the rl-suite-restricted-path-42n Panel: the run header's second copy of CNF-1's list; a failed ln for a non-needed name is silent; banner width","description":"Three nits from the opus Reviewer, Round 3 of rl-suite-restricted-path-42n, none a defect:\n\n- `conformance/run` line 5 carries a second copy of `CNF-1`'s tool list (\"Needs bash, git, GNU\n  coreutils, grep, sed, awk, cmp and find; nothing else\"), hand-updated in that Run — the same\n  duplication the Run removed from `CNF-2`. \"Needs the tools `CNF-1` names; nothing else.\" ends it.\n- In `restrict_path` (`conformance/scenario-lib.sh`), a failed `ln -s` for a name not in\n  `executable_path_needs` is silent: the name is neither linked nor in `skipped`, so the private\n  directory quietly lacks it. `ln -s … || skipped=\"$skipped $name\"` makes the skip list true in\n  both cases. Companion to rl-cnf2-restrict-path-skip-check-sqo, which wants a check for the\n  skip and refusal paths.\n- The `# --- CNF-2: the executable's PATH ---` banner in `scenario-lib.sh` is 102 columns against\n  the file's 97.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T19:05:47.431441529Z","created_by":"master","updated_at":"2026-09-24T19:05:47.431441529Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf31-nul-pick-twin-uwq","title":"CNF-31 lacks the pick-site twin of CNF-26's NUL-split probe shape","description":"Follow-up from the Implementer and Panel of Run 20260924T220258.966885019Z-494739\n(rl-exact-bytes-nul-gitignore-pjc, commit fd1d87c). Nothing is red.\n\n`CNF-26` now has the `nul` probe shape (`conformance/fakes/agent`): `Current week (Fable): 1`,\na NUL byte, `00% used …`, which must leave every Seat `unknown`. `CNF-31`'s pick-site list of\nfail-open shapes (non-zero exit after the `Fable` line, mid-line words, `99% used`, nothing,\nunlisted families, aggregate only, no usage line, the bound) has no such row. A NUL-dropping\nparser at the pick site reads `fable:unavailable` and, with the default models, refuses the Run\nunder `RUN-22` — the fail-closed direction `RUN-21` exists to prevent — and no row sees it.\nrloop-bash `02c6206` shares one parser between the two call sites, so `CNF-26` catches it there;\nnothing in the set requires the sites to share a parser, which is the only reason the twin has\nany value.\n\n## What to change\n\n- Add the `nul` shape to `CNF-31`'s pick-site `for` loop in `conformance/run` (the Run starts\n  and ends 0 with every Seat `unknown` in `probe-pick.md`, on the default models), and to the\n  item's prose list in `06-conformance.md`.\n- Demonstrate (`F5`) against a scratch copy of rloop-bash `02c6206` whose pick-site parser drops\n  NUL bytes while its Round parser does not (the two-parser mutant is what the row is for); the\n  recipe for a copy that finds `cmp` on the suite's private PATH is in the Run's `task.md`.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; the new row is red\nagainst that mutant and green against a fixed copy; no other item changes.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T22:31:49.491032984Z","created_by":"master","updated_at":"2026-09-24T22:31:49.491032984Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-pjc-panel-nits-99e","title":"Nits from the rl-exact-bytes-nul-gitignore-pjc Panel: RUN-4's exemption says create where DIR-2 now rewrites; fake header ';' and width; CNF-8 loop's printf format and spaced label; a tracked .rloop/.gitignore","description":"Suggestions from the Panel of Run 20260924T220258.966885019Z-494739\n(rl-exact-bytes-nul-gitignore-pjc, commit fd1d87c). Nothing is red; each is cosmetic or a\nwording gap in what the Round accepted.\n\n1. `01-run-lifecycle.md` `RUN-4`: its exemption says rloop `MUST NOT create any other file outside\n   it, save the .rloop/.gitignore DIR-2 names`. `DIR-2` now also mandates *rewriting* a\n   pre-existing `.rloop/.gitignore` whose bytes differ. `DIR-2` is the specific rule and wins,\n   but the exemption's verb no longer covers everything `DIR-2` requires. Widen the verb (create\n   or rewrite) or cite `DIR-2`'s rewrite in the same clause; refresh the reviewed clause's\n   fingerprint in `tools/restatement-homes.json` if `RUN-4` is one.\n2. `conformance/fakes/agent` header, the `RLOOP_FAKE_PROBE` list: the `unlisted` entry is the only\n   one without a trailing `;`, so it runs into the new `nul` line. One character.\n3. `conformance/fakes/agent` header line 8 (`RLOOP_FAKE_PICK`) is about 105 columns; wrap it like\n   the `RLOOP_FAKE_PROBE` list.\n4. `conformance/run`, the `CNF-8` pre-existing-shape loop: `printf \"${shape#*:}\"` uses loop data\n   as a `printf` format string (`printf '%b'` is sturdier), and the label `two newlines` carries\n   a space into the record path `$work/rec-default-two newlines`; every other `rec` name in the\n   suite is space-free. Neither bites today.\n5. A tracked `.rloop/.gitignore` (one force-added with `git add -f`) would be dirtied by\n   `DIR-2`'s rewrite; the suite's repositories never track it, so it does not bite there. Record\n   it in `07-open-findings.md` if the owner wants it written down, or leave it.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; no suite behaviour\nchange; `conformance/test-panel-trace` with no argument passes.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T22:31:49.523052480Z","created_by":"master","updated_at":"2026-09-24T22:31:49.523052480Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-bim","title":"Nits from the rl-gan Panel: which mutant reddens which control half; CLAUSE placement and its duplicate; three citation runs over one file; F2's eight-line sentence; the four-line norm() docstring","description":"Suggestions from the Panel of Run 20260924T223240.212906000Z-494739 (rl-gan, commit 2e72f78). Nothing is red; each is a wording or shape nit in what the Round accepted.\n\n1. tools/test_citation_gates.py, the three normalizer controls (test_strikethrough_never_changes_a_digest_or_a_quotation, test_underscore_emphasis_is_a_changed_word, test_literal_code_characters_follow_the_same_rule). unittest stops a method at its first failing assertion, so the brief's two mutants (norm stripping underscores too; norm keeping tildes) redden only the first half of each method. The Manager showed the strikethrough negative half (a changed word inside ~~...~~ is still reported) red under a third mutant that deletes ~~...~~ spans before stripping (re.sub(r'~~.*?~~', '', text, flags=re.S)); the underscore positive half (a quotation whose underscores are in the owner's body verifies) cannot go red under any normalizer that treats both sides alike, which is inherent to an equality control, not a weak fixture. Add one comment line per method naming which mutant reddens which half, so a later reader does not assume every assertion is mutation-covered; or split each method in two so a mutant run names the half.\n\n2. Same file: CLAUSE sits mid-class between two test methods; the file's fixtures (QUOTE, FALSE_QUOTE) are module-level. Move it there and let test_reviewed_home_cannot_gain_a_new_duty, which spells the same RUN-21 sentence out literally, use it.\n\n3. Same file, the literal-code negative: three check_citations.py runs over one unchanged control.md to assert three findings; one run asserting all three is a third of the subprocesses (the file already does the three-run shape near test_quote_adjacency_required_examples, so this is in keeping either way).\n\n4. 07-open-findings.md F2, the digest paragraph: the character rule is one sentence of about eight lines carrying three semicolon-joined rules. Breaking it at the semicolons changes nothing it states.\n\n5. tools/spec_text.py norm() docstring is four lines; the brief asked for two or three. Trimming '(F2 states why this is the boundary)' to 'see F2' fits three.\n\nAcceptance: nix develop --command bash tools/check-all.sh exits 0, unpiped; nix develop --command python3 tools/test_citation_gates.py exits 0; no behaviour change in norm() or the gates; the three controls still red under the brief's two mutants and the span-dropping third, demonstrated in a /tmp copy (F5).","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T22:53:22.179200719Z","created_by":"master","updated_at":"2026-09-24T22:53:22.179200719Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-sol-gpt-6-uxw","title":"Move the sol Reviewer and adviser from gpt-5.6-sol to gpt-6-sol (AGT-10, PRM-1, PRM-2)","description":"Move the `sol` Reviewer and adviser from `gpt-5.6-sol` to `gpt-6-sol`.\n\nOwner's decision, 2026-09-24. `gpt-6-sol` is available on this account and answers through the\ncodex CLI (`codex exec --dangerously-bypass-approvals-and-sandbox -m gpt-6-sol -c\nmodel_reasoning_effort=low \"Reply with exactly: ok\"` returned `ok`). The roster name `sol` does not\nchange; only the model id behind it does, in every place the Specification names it.\n\n## What to change\n\nEvery occurrence of `gpt-5.6-sol` in the set, as of `6fc3ebd`:\n\n| where | what |\n|---|---|\n| `02-agents.md` `AGT-10`, sol's command line (line 168) | `-m gpt-6-sol`; effort stays `xhigh`. Add the dated italic note in the house style: what changed, when, why. |\n| `03-prompts.md` `PRM-1` (line 28) and `PRM-2` (line 66), the Consultation's adviser command line | `-m gpt-6-sol`. These are prompt bodies, byte-exact under `ADR-0001`. |\n| `docs/adr/0006-…` line 19 | the list of model ids the Probe's family table does not cover; update the id. |\n| `conformance/fakes/agent` line 214 | the fake's model-name match; add `gpt-6-sol` (keep `gpt-5.6-sol` too if the fake must accept an older Implementation's argv — decide by reading `CNF-2`'s contract; if the fake only ever sees what this suite spawns, replace). |\n| `conformance/fixtures/argv/AGT-10-2.txt`, `conformance/fixtures/prompts/PRM-1.txt`, `conformance/fixtures/prompts/PRM-2.txt` | regenerated by `python3 tools/check_fixtures.py --write`; never edited by hand. |\n\nCheck `README.md`, `CONTEXT.md` and `07-open-findings.md` for the old id and update any mention;\n`F`-entries that record a past incident keep the id they observed.\n\n## Acceptance\n\n- `nix develop --command bash tools/check-all.sh` exits 0, unpiped, every gate PASS; the fixtures\n  gate reports the three fixtures as the documents' blocks.\n- `grep -rn gpt-5.6-sol` over the set, excluding `.rloop/` and `.beads/`, finds only historical\n  `F`-entries or nothing.\n- `CNF-11` and `CNF-12` go red against rloop-bash `02c6206` on the sol argv and the two prompts,\n  and green against a scratch copy with the three ids changed (`F5`, demonstrate both). The\n  Implementation used is `/home/master/p/rloop-bash` (github:douglaz/rloop-bash); use a scratch\n  copy of its `bin/rloop` under `/tmp`.\n- One live call to `gpt-6-sol` through the `AGT-10` command line, on a trivial prompt, answers\n  (record the output in the Finished File; this is `CNF-22`'s kind of evidence, not an item).\n\n## Not in scope\n\nrloop-bash's own change: its `bin/rloop` Reviewer arm and two comment lines name the old id and\nmust move with the next pin bump; that is an instruction to its next Run. Any other roster change.\n","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-24T22:54:41.316530645Z","created_by":"master","updated_at":"2026-09-24T23:18:12.490013371Z","closed_at":"2026-09-24T23:18:12.489961574Z","close_reason":"Done in 6b789b5: AGT-10, PRM-1, PRM-2, ADR-0006 and the fake name gpt-6-sol; fixtures regenerated; live xhigh call answered ok on codex-cli 0.156.1; Panel nits filed as rl-8jr","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-8jr","title":"Nits from the rl-sol-gpt-6-uxw Panel: AGT-10's note quotes the low-effort probe, not the xhigh line it annotates; 'the gpt-5.6 model of that name'; AGT-17 lags codex-cli 0.156.1","description":"Filed by the Manager of Run 20260924T225454 after accepting rl-sol-gpt-6-uxw (commit on master, 2026-09-24). Four Reviewers, no defect in the substitution; these are wording nits and one stale clause.\n\n1. The dated note under `AGT-10` (02-agents.md, after the `codex review --base` note) quotes the owner's `model_reasoning_effort=low` probe as evidence that `gpt-6-sol` answers. The line it annotates is `xhigh`, and the Implementer, fable and opus each ran that exact line live on codex-cli 0.156.1 and got `ok`, exit 0. Quote the `xhigh` call instead.\n\n2. The same note says sol `ran on the gpt-5.6 model of that name` because the Run's acceptance sweep forbade the literal old id anywhere in the set. That rule was the brief's, not the set's: `F`-entries keep the ids they observed, and a history note is the same kind of record. Name `gpt-5.6-sol` literally in the note; a future sweep excludes that line.\n\n3. `AGT-17` still says the command lines `were verified against Claude Code 2.1.274 and codex 0.153.4 on 2026-09-17/18`. sol's line as it now reads has only been verified on codex-cli 0.156.1 (2026-09-24), and the Manager's Run used Claude Code 2.1.282. Refresh AGT-17 the next time a live Run exercises the whole roster, or record the partial verification now; the note's `AGT-17's verified versions did not` sentence should then carry AGT-17's words (AGENTS.md, Quote the sentence).\n\nNot a spec defect, for the rloop-bash pin bump instead: any edit to PRM-1 or PRM-2 reddens every pick and judge label of CNF-11 and CNF-12 and all four Runs of CNF-32 against an unbumped Implementation, because the Manager's prompt is an argv element and CNF-32 compares the pick prompt byte for byte; a Round's brief that predicts a per-label delta must count those.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-24T23:17:39.911341120Z","created_by":"master","updated_at":"2026-09-24T23:17:39.911341120Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-csw","title":"Nits from the rl-cnf29-not-named-stream-pg8 Panel: 'presence only' is of standard error, not the item; the two quotations under the citations gate; the repeated probe-pick.md literals; CNF-23 reads one judge argv, says every","description":"Suggestions from the Panel of Run 20260925T024030.524416029Z-367594 (rl-cnf29-not-named-stream-pg8),\nall from the opus Reviewer. Nothing is red; each is a wording or reach nit in what the Round accepted.\n\n1. `06-conformance.md`, `CNF-29`: the new sentence `The item asserts presence only` is unqualified,\n   but the item's first sentence asserts three absences — `no Manager, Implementer or Reviewer\n   argument list recorded`, `standard output empty`, `the Run Directory holding the three\n   probe-pick.* files alone` — and the rationale rests on one of them (`the spawn record, not the\n   status`). Both justifying quotations are about the stream. Qualify it: `Of standard error the\n   item asserts presence only: …`. (The brief asked for the unqualified phrasing; the error is the\n   Manager's, not the Implementer's.)\n2. Same sentence: neither the deleted sentence nor the new one is detected by `spec_text.attributions`,\n   so the \"Quote the sentence\" rule is enforced here by review only. Phrasing the quotations as\n   `RUN-22` states `…` / `RUN-10` states `…` brings both under the citations gate at the blocking\n   tier; the opus Reviewer checked a scratch copy and the gates stayed green with no new advisory.\n   It also drops the nested em-dashes around a quotation that itself contains em-dashes.\n3. Same item: `manager:unknown` / `implementer:unavailable` now appear twice, as the assertion\n   (line ~299) and again in the rationale (line ~311). Point the rationale back at the assertion\n   (\"in `probe-pick.md` above\") instead of repeating the literals.\n4. `conformance/run`, `CNF-23`: the pass message says `every judge call carries it`, but the read\n   is `ls \"$rec\"/*-manager-1.argv | head -1` — one argv, not every. Either loop over the judge\n   argv files or make the message say what is read. The Round's guard touched that line without\n   changing its reach, as the brief asked.\n\nAlso recorded: on the Step C mutants that edit rloop-bash's shared `refuse()` message (C3, C4, C5),\n`CNF-28` and `CNF-30` redden too, since they read the same `err` line; the Implementer's report\nnamed only the extra `names` labels on C1 and C2. Mutant-inherent, no suite change.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; no requirement or\nsuite behaviour change; the suite against /home/master/p/rloop-bash/result/bin/rloop (02c6206)\nreports the same verdict list as before, `CNF-23`'s pass message aside if item 4 changes it.\n","notes":"Item 1 (the 'presence only' qualifier) applied on 2026-09-25 in the commit after the closing one — the closing commit's message claimed it early, the substitution had missed the wrapped line. Items 2 to 4 stand.","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T03:15:54.274990196Z","created_by":"master","updated_at":"2026-09-25T09:26:47.352995179Z","closed_at":"2026-09-25T09:26:47.352943502Z","close_reason":"All four items landed: item 1 (the 'presence only' qualifier) earlier, items 2-4 in this Run. The two CNF-29 quotations are attributed to RUN-22 and RUN-10 at the citations gate's blocking tier (demonstrated red by mutation on disposable copies), the record literals appear once in the assertion, and CNF-23's pass message was narrowed to the one judge call the block reads. Gate exit 0, 0 blocking and the one pre-existing ADR-0006 advisory; suite verdict list unmoved. The Panel's two reach suggestions are rl-cnf23-every-judge-argv-zq6.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-prm2-foreground-judge-88k","title":"PRM-2 lacks the foreground-only sentence: a judge that runs a check in the background ends its turn with no decision (F16's judge twin)","description":"On 2026-09-25 the judge of Run 20260925T024030.524416029Z-367594 (rl-cnf29-not-named-stream-pg8) wrote 'The suite is halfway; the rest of my work needs its final verdict list, so I'll wait for its completion notice.' and ended its turn. rloop applied RUN-12 (no decision: task.md unchanged, no Finished File) and exited 2, leaving the Round's accepted work uncommitted and the bead claimed; the Sequence stopped. This is F16's failure in the Manager's seat: rl-agents-end-turn-on-background-check-twn (Run 20260923T050941) added 'Run every command in the foreground and wait for it to finish; do not use your shell tool's background facility' to PRM-3 and PRM-4 only. PRM-1 and PRM-2 never got it, and the judge is the call whose lost turn costs the most: RUN-12 ends the Run and, under --auto, the Sequence.\n\nWhat to change: add the same sentence to PRM-2 (and to PRM-1, where a pick that backgrounds the gates would end the same way under RUN-11's no-decision row), placed where PRM-3 places it; regenerate the two prompt fixtures with python3 tools/check_fixtures.py --write, never by hand; extend F16 with this observation, dated, and the Run id. rloop-bash re-splices the prompts on its next pin bump (its own repository's work).\n\nAcceptance: nix develop --command bash tools/check-all.sh exits 0, unpiped, every gate PASS; the fixtures gate reports PRM-1.txt and PRM-2.txt as the documents' blocks; CNF-12 goes red against rloop-bash 02c6206 on the two prompts and green against a scratch copy carrying the new bytes (F5); grep over 03-prompts.md finds the sentence in all four prompts.","status":"closed","priority":2,"issue_type":"task","created_at":"2026-09-25T03:17:26.221049737Z","created_by":"master","updated_at":"2026-09-25T03:46:25.355959040Z","closed_at":"2026-09-25T03:46:25.355911260Z","close_reason":"Done in 526b0ed and 1de847e: PRM-1 and PRM-2 carry PRM-3's foreground-only sentence before PRM-5's closing one; PRM-1.txt and PRM-2.txt regenerated by the tool; F16 records the judge of Run 20260925T024030 as a fourth observation, count-free, quoting RUN-12. rloop-bash adopts the bytes on its next pin bump; note for that bump: its 02c6206 script still says gpt-5.6-sol at lines 31 and 62, which already reddens every pick/judge label of CNF-11/12 and CNF-32, so the F5 demonstration here used sol-fixed scratch copies. Follow-ups: rl-mm1 (03-prompts.md 'five prompts'), F16 wording nits bead.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-mm1","title":"03-prompts.md line 3 says 'The five prompts, verbatim' where the document holds four prompt blocks and four fixtures","description":"Pre-existing since c0522e3, reported by the opus Reviewer of Run 20260925T031827 (rl-prm2-foreground-judge-88k). AGENTS.md: cite the list; let it hold the number. Drop the count ('The prompts, verbatim.') or make it four; check_fixtures.py counts the blocks, so no gate catches it.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T03:40:37.820547951Z","created_by":"master","updated_at":"2026-09-25T03:40:37.820547951Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-rk7","title":"F16 wording nits from the rl-prm2-foreground-judge-88k Panel: 'Each observation below is a claude Seat' equates an observation with a Seat; 'One observation of the prohibition complying' is a count in prose","description":"Reported by the opus Reviewer of Run 20260925T031827, Round 2. 07-open-findings.md:412 could read 'is of a claude Seat'; line 447's 'One observation' will restale when a second compliance is recorded (AGENTS.md: cite the list; let it hold the number). Cosmetic; touch with the next F16 edit.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T03:46:25.319807487Z","created_by":"master","updated_at":"2026-09-25T03:46:25.319807487Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-aa4-nits-bxq","title":"Fourth CNF-11 Run: make the off-table invariant a checked guard; two label/comment nits (from the rl-aa4 Panel)","description":"Suggestions from the opus Reviewer on Round 2 of Run 20260925T034709.676726212Z-1798353 (rl-aa4,\nthe fourth CNF-11 Run). Nothing is red; filed by hand because that Round's judge was lost at its\nbound.\n\n1. **Make the fourth Run's invariant checkable rather than commented.** Its seven assertions are\n   live only while `off_table_model` (`conformance/scenario-lib.sh`) is a model no Seat defaults\n   to; nothing enforces that, and setting it to `claude-opus-5` would silently turn the `opus`\n   check back into decoration. One line before the Run: a `case \"$off_table_model\" in` listing\n   every Seat's default and fixed model, exiting the suite with its own diagnostic on a match.\n   Demonstrate red by setting it to `claude-opus-5` on a scratch copy (F5).\n2. **Exit-label wording** (`conformance/run`, the fourth Run's exit assertion): the other Runs\n   label their exit by name (`[run 1: …]`, `[run 2 (codex implementer): …]`, `[hostile: …]`); this\n   one reads `[--implementer-model claude-sonnet-5: exit …]`. Name it the same way.\n3. **The comment's `Without the flag an Implementation spawns nothing here`** can be misread as the\n   Run not passing the flag. Say `an Implementation that does not accept the flag spawns nothing\n   here (AGT-2)`.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; item 1's guard\ndemonstrated red on a scratch copy; every CNF item's verdict unchanged against\n`/home/master/p/rloop-bash`'s `result/bin/rloop` (github:douglaz/rloop-bash) apart from item 2's\nlabel text.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T07:31:22.894941050Z","created_by":"master","updated_at":"2026-09-25T07:31:22.894941050Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-agt18-run21-twice-syk","title":"AGT-18's paragraph points at RUN-21 twice in consecutive sentences","description":"Suggestion from the Panels of Run 20260925T073142.501464046Z-2268035 (rl-agt18-probe-seats-po1),\nraised by fable and opus. Nothing is red; it is a wording nit in what the Round accepted.\n\n`AGT-18`'s second paragraph now hands off to `RUN-21` twice in consecutive sentences:\n`what each Seat then reads is RUN-21's. There is no codex command line here; RUN-21 says what\nfollows from that.` The trailing clause predates the amendment and the brief said to leave it, so\nthe Round was right not to touch it, but the paragraph lands the same pointer twice in eleven\nwords.\n\nFold the two. One shape the Panel offered: end the paragraph at `There is no codex command line\nhere.`, since the preceding clause already points at `RUN-21`. Check before dropping the clause\nthat no requirement or `CNF` item depends on `AGT-18` citing `RUN-21` for the codex consequence\nspecifically — `tools/check_coverage.py` and `tools/check_citations.py` are what would say so.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; `AGT-18` states the\ncodex consequence once and points at `RUN-21` once; no requirement or suite behaviour change; no\nfixture changes (the `claude -p \"/usage\"` block is untouched).\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T07:36:29.001678827Z","created_by":"master","updated_at":"2026-09-25T07:36:29.001678827Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-adr0006-quota-line-citation-5tw","title":"ADR-0006:11 attributes RUN-21's quota line to AGT-18 (standing citation advisory)","description":"Suggestion from the Panels of Run 20260925T073142.501464046Z-2268035 (rl-agt18-probe-seats-po1),\nraised by fable and opus. Pre-existing and not that Round's: the advisory stands at 154a414 too,\nand the sentence that Round removed from `AGT-18` never held the quoted line.\n\n`tools/check_citations.py` reports, on every run of the gate:\n\n```\nADVISORY CITATION: docs/adr/0006-rloop-reads-a-vendors-quota-report-and-fails-open.md:11\nattributes to AGT-18 (02-agents.md) words absent from its body\n```\n\nThe line is ADR-0006's `So rloop runs the probe AGT-18 states before each Panel and reads the\nCurrent week (<family>): <n>% used line out of it (RUN-21).` The quoted format belongs to\n`RUN-21`, which the sentence does cite at its end; the nearest-citation fallback reaches `AGT-18`\nfirst because that identifier stands closer to the quotation. `02-agents.md` `AGT-17` already\ncredits the line to `RUN-21`.\n\nRe-order the sentence so the quotation's nearest citation is its owner — for example, put\n`RUN-21` beside the quoted line rather than at the sentence's end — and confirm the advisory is\ngone from the gate's output. Do not move the rule itself: `RUN-21` owns it and ADR-0006 cites it.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, and no longer prints\nthat ADVISORY CITATION line; ADR-0006's argument is unchanged; no requirement changes.\n\n`rl-bs8` also edits ADR-0006 (its first consequence); whichever lands second rebases.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T07:36:38.186654641Z","created_by":"master","updated_at":"2026-09-25T07:36:38.186654641Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf32-starts-ends0-nits-hur","title":"CNF-32 wording nits from the rl-cnf32-f4-wording-gke Panels: 'each of which starts' under-describes started(); 'Run' twice in one clause","description":"Suggestions from the Panels of Run `20260925T073720.252529547Z-2268035` (`rl-cnf32-f4-wording-gke`).\nNothing is red; both are wording nits in one sentence of `CNF-32`, raised by the fable and opus\nReviewers and not asked for by that Run's brief.\n\n1. `06-conformance.md` `CNF-32` says its four Runs are `four Runs of this item's own, each of which\n   starts`. The helper the suite applies to all four, `started()` (`conformance/run`), asserts more\n   than a start: exit 0, a recorded pick argv and a Finished File. `CNF-31` spells its version out —\n   `Seats off the table, or a probe before the pick that reads unknown, start the Run` in the verdict,\n   and in the item `the Run starts` with its own assertion of the exit. Consider `each of which\n   starts and ends 0`. The base text under-described `started()` the same way, so this is not a\n   regression.\n2. In the same item, `Each Seat has a Run of its own because one Run cannot tell a working render\n   from one that hardcodes a Seat` uses \"Run\" twice with different referents in one clause. `no\n   single Run can tell` removes the stutter. (The clause reads correctly as it stands; the first\n   \"Run\" is the one that varies, the second the one that cannot decide alone.)\n\nAlso noted and **not** filed: `conformance/run`'s comment `Each Run must start (CNF-31's started())`\nin the `CNF-32` block was read as a defect by neither Reviewer that raised it — the helper is\ndefined under `CNF-31`, so the citation is where the reader should look.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; no requirement or\nsuite behaviour change; `CNF-32` stays inside 100 columns.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T07:47:30.835618928Z","created_by":"master","updated_at":"2026-09-25T07:47:30.835618928Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-a5n-panel-nits-dgb","title":"test-panel-trace and fake-header nits from the rl-rounds-fallthrough-a5n Panel: the header's omitted-field claim; one-assertion F5 control; a control red for any reason; no Round 1 guard on the opus probe arm","description":"Suggestions from the Panel of Run 20260925T074815.482543987Z-2268035\n(`rl-rounds-fallthrough-a5n`, commit `035644f`). Nothing is red: all four Reviewers reported no\nfindings, the gates exit 0 and the suite's verdict against `rloop-bash` `02c6206` is unchanged.\nEvery item below is in `conformance/test-panel-trace` or `conformance/fakes/agent`.\n\n1. The header entry overreaches on one of its two cases (opus). `conformance/fakes/agent:11-15`\nsays a Round past the last group **and** a field its group omits give the failing defaults,\n`which is what Scenarios.lean's behaviour defaults to there`. That is exact for the first case\nand not for the second: `behaviour` indexes whole `RoundChoice`s, so the model has no\npartially-scripted Round to default. The commit message has it right — \"the model has no\nrepresentation for a partially-scripted Round\" — and moving that clause into the header makes\nthe two homes agree.\n\n2. The F5 control proves one assertion of three (fable, opus). The scratch copy restores all\nthree old defaults at once and `rounds_case` dies at its first failed `assert_equal`, so the\ncontrol never reaches the Reviewer or the judge assertion. Both Reviewers built single-site\nrestorations by hand and each was red at its own assertion (`o:-ok` -> `Round 2 Implementer`,\n`p:-all` -> `Round 2 Reviewer`, the judge default removed -> `Round 2 judge`), so the teeth are\nthere; the file does not prove it. A loop over three single-site `sed` copies would, at about\nthe same length.\n\n3. The control accepts any non-zero exit as red (opus). `conformance/test-panel-trace:159`,\n`( rounds_case \"$work/old-defaults\" ) 2>/dev/null && die …`: a `chmod` that did not take or a\n`sed` that produced an unparseable script also exits non-zero and leaves the control green while\nproving nothing — the failure mode `probe_case`'s `cmp -s … && die 'the mutation changed\nnothing'` guards against. Capturing the message and matching it against `FAIL: Round 2\nImplementer*` closes it in the same line count.\n\n4. The widened probe arm has no Round 1 guard (opus). `:116` asserts the `fable` shape differs\nfrom `clear`, which is what makes `:117` mean something; nothing asserts the same of `opus-1`.\nIf the `opus` shape ever collapsed onto `clear`, `:124` would misfire with `Round 3 repeats the\nscript's last shape` instead of naming the real breakage. One `cmp -s \"$work/probe/opus-1\"\n\"$work/probe/clear\" && die 'Round 1 of opus writes clear'` makes the two arms symmetric.\n\n5. `Round 2 judge wrote a Finished File` has no mutant (fable). No restoration of the old\ndefault reddens it: the old judge default wrote nothing and exited 0. The mutant would be a fake\nthat exits 1 after writing the file. Worth it only if F5 coverage is wanted per assertion rather\nthan per control; take or leave with item 2.\n\n6. Take or leave (fable): `cmp -s late-3 opus-1 && die` at `:124` is implied by the two `cmp`\ncalls against `clear` (`late-3 = clear` and `clear != opus-1` already hold). The brief asked for\nit in those words, so it is not wrong — only redundant.\n\nAcceptance: whatever lands is demonstrated, not asserted — each new or changed assertion shown\nred against a scratch copy (`F5`); `nix develop --command bash tools/check-all.sh` exits 0,\nunpiped, with the two pre-existing advisories and no new one; `conformance/scenarios.tsv` and\n`conformance/run` unchanged; the suite's verdict against `rloop-bash` `02c6206` still\n`19 passed, 11 failed` with the same eleven names.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T08:08:29.921182632Z","created_by":"master","updated_at":"2026-09-25T08:08:29.921182632Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-probe-some-panel-nits-0ai","title":"Nits from the rl-probe-block-some-class-1fx Panel: positive()'s 'complete Panel' label on a three-member Panel; the header's mutation-site inventory; CNF-27's 'nothing else exercises the two kinds together'; narrowProbe's 'whatever the probe took out'","description":"Nits from the `rl-probe-block-some-class-1fx` Panel (opus, Run\n`20260925T080910.224323832Z-2268035`, commit that closed it). Each is prose the change made\ninaccurate; none affects a gate's verdict. Line numbers move — re-derive before editing.\n\n- `conformance/test-panel-trace`, `positive()`: the helper hardcodes the word `complete Panel`\n  into both its assertion label and its PASS line, because it was written for the three\n  probe-unscripted lines whose Panel is all four Reviewers. The new `some`-with-an-uncalled-Reviewer\n  control reuses it, so the gate prints `PASS: S210 complete Panel, exit 0, expected = observed:\n  pick,impl1:ok,panel1:some:astra+opus+sol,judge1` — a three-member Panel announced as complete,\n  contradicted by the trace three words later. The reuse is right; take the label as an argument,\n  or drop the claim and say `expected = observed` alone.\n- `conformance/test-panel-trace`'s header: `The mutants are pinned to rloop-bash's source, its two\n  literal Reviewer loops in do_round and its probe reading (RUN-21)` now enumerates one site short.\n  The new mutant pins a third, the zero-survivor abort `[ \"$failed\" -lt 4 ]`, and its replacement\n  text also depends on the `pids` array's literal shape. The header is what tells a reader why the\n  argument must be the rloop-bash script and what breaks when rloop-bash moves.\n- `conformance/run`, the `rec-omitted-abort` block of `CNF-27`: `Nothing else exercises the two\n  kinds together` is now false on a strict reading — the generated probe block reaches a Round with\n  an uncalled Reviewer beside a failed called one (`S209`, `S210` and their kin). What the sentence\n  is really claiming is that nothing else makes the two kinds *all down* together, which is still\n  true; narrow it to that.\n- `tools/formal/Rloop/Scenarios.lean`, `narrowProbe`'s docstring: `.some` is \"not all down\n  **whatever the probe took out**\". `Loop.lean`'s `allDown` is `called.isEmpty || p == .none`, so a\n  probe that took every Reviewer out would be all down. True for `probeReads`, which can remove\n  only `fable` and `opus`, but the condition is `while some Reviewer is still called` and saying so\n  costs nothing.\n\n`conformance/test-panel-trace`'s new lines are also 168 and 113 columns where the file wraps near\n100; `rl-0pj` owns that file's width nit and says to re-derive with `awk 'length>98'`, so these\nfall to it rather than here.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T08:36:18.237166998Z","created_by":"master","updated_at":"2026-09-25T08:36:18.237166998Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-readme-gates-every-push-ung","title":"README's Gates lead-in says CI runs the gates on every push; the workflow runs on master/main pushes, pull requests and dispatch","description":"Nit from opus on the Panel of Run 20260925T083727.810322153Z-2268035 (rl-0pj), against README.md as\nof 64102ff. The Gates section's lead-in says `nix develop --command bash tools/check-all.sh` runs\nevery gate below, and CI runs the same script on every push` (README.md:16-17). The workflow does\nnot trigger on every push: .github/workflows/gates.yml:9-13 is `on: push: branches: [master, main]`\nplus `pull_request` and `workflow_dispatch`, so a push to a feature branch with no pull request open\nruns nothing at all.\n\nTwo things to settle when this is picked, neither a defect in the workflow: whether the sentence\nshould say which pushes (and then it moves whenever the branch list does — see AGENTS.md, 'Cite the\nlist; let it hold the number'), or whether it should stop claiming a trigger at all ('CI runs the\nsame script'). Same sentence also carries 'every gate below', which is accurate.\n\nWhole-README, low priority. Prose only: the workflow's triggers are not in scope, and rl-5mk owns\nits last step.\n","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T09:06:39.824130971Z","created_by":"master","updated_at":"2026-09-25T09:06:39.824130971Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf23-every-judge-argv-zq6","title":"CNF-23 reads one judge argv where the item asserts every; head -1 would miss a retried judge call","description":"Suggestions from the Panel of Run `20260925T090746.368477655Z-2268035` (`rl-csw`), raised by the\nopus Reviewer. Nothing is red; both are reach gaps that Round's accepted narrowing made visible.\n\nThat Round settled `CNF-23`'s pass message against the read rather than the other way round: the\nmessage now says `the one Round's judge call carries it` (`conformance/run`), which is true of the\nscenario, scripted `RLOOP_FAKE_ROUNDS=\"impl=ok;panel=all;judge=done\"` — one Round, one judge\nargument list.\n\n1. `06-conformance.md` `CNF-23` asserts `every judge call is given that same id`, and the suite now\n   says out loud that it reads one. In a one-Round Run the two coincide, so nothing is unverified\n   today, but no `CNF` item exercises the property over a Run with more than one judge call. The\n   cheap close: script a second Round, loop over every `*-manager-*.argv` whose round is at least 1,\n   and let the pass message say `every judge call` again.\n2. `conformance/run`'s read is `ls \"$rec\"/*-manager-1.argv … | head -1`, the first match. The fake\n   prefixes each record with a sequence number (`conformance/fakes/agent`), so an Implementation\n   that retried a judge call would write two `*-manager-1.argv` files and only the earlier one would\n   be read — under which even the narrowed message overclaims. Pre-existing, and reachable only by\n   an Implementation doing something no requirement permits; recorded so it is not rediscovered.\n\nWhichever shape is taken, item 1's loop subsumes item 2's `head -1`.\n\nAlso noted and **not** filed: the same Reviewer read `Of standard error the item asserts presence\nonly.` in `CNF-29` as having lost the colon that made the two following sentences its support\n(`presence only, because …` was the offered shape). Taste; the substance is intact and the two\nquotations' blocking attribution shapes depend on what follows the verb, not on that punctuation.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped; `CNF-23`'s pass\nmessage describes what the suite reads; the suite's verdict list does not move except for that\nmessage.\n","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T09:26:41.401395095Z","created_by":"master","updated_at":"2026-09-25T09:52:15.168252917Z","closed_at":"2026-09-25T09:52:15.168198885Z","close_reason":"conformance/run 9159dc8: the third argv Run judges rewrite then done and CNF-23 loops over every *-manager-[1-9]*.argv, with a count guard against a silent revert to one Round; follow-ups in rl-cnf11-judge-id-second-home-93a","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf11-judge-id-second-home-93a","title":"CNF-11's 'every judge call carries that same id' is a second home for CNF-23's property; CNF-23's prose does not name its two-Round shape","description":"Suggestions from the Panel of Run `20260925T092734.145398704Z-2268035`\n(`rl-cnf23-every-judge-argv-zq6`, commit `9159dc8`), raised by the opus Reviewer. Nothing is red:\nthe gates exit 0 and the suite's verdict against `rloop-bash` `02c6206` is `19 passed, 11 failed`\nwith the eleven names it had before. All three are prose or reach, in `06-conformance.md` and\n`conformance/run`.\n\n1. `CNF-11` carries a second home for a property `CNF-23` owns. `06-conformance.md`\n   `CNF-11` says the pick's `<session id>` is the `session` file's content `and every judge call\n   carries that same id`; `CNF-23` says `every judge call is given that same id` and is the\n   requirement's home — it is the item that now reads them all (`conformance/run`, the loop over\n   every `*-manager-[1-9]*.argv` of the third argv Run). `CNF-11`'s block still reads one judge\n   call per Run, and the third Run now makes one it does not read, so the sentence over-describes\n   what that item checks. Per `AGENTS.md`'s **Cite the owner**, narrow `CNF-11`'s clause to the\n   call it reads and point at `CNF-23` for the rest. `CNF-11`'s assertions in the suite were out\n   of scope for that Run and stay out of scope here unless the prose forces them.\n\n2. `CNF-23`'s prose does not name the two-Round shape the suite now runs. The only thing holding\n   the suite to more than one judge call is the block's count guard, `[ \"$judges\" != 2 ]`. The\n   Round left this deliberately — the item's `every judge call` is true of any Run — but `CNF-24`\n   sets the precedent for naming a shape in the item: `A Run of two Rounds leaves probe-pick.*,\n   probe-1.* and probe-2.* and no probe-0.*`. One sentence either way; take it with item 1, since\n   both are the same document's prose about the same Run.\n\n3. Recorded, not a defect: `conformance/run`'s `check_call` reads `recf=$(ls $1 2>/dev/null | head\n   -1)`, the first match, for every role of every argv Run — the same first-match shape that\n   `rl-cnf23-every-judge-argv-zq6`'s second item recorded for the judge and that its loop closed\n   for `CNF-23` alone. Reachable only by an Implementation that spawns one role twice, which no\n   requirement permits; `:422`, `:551`, `:555`, `:589` and `:663` read the same way. Noted by the\n   Implementer of that Round and filed here so it is not rediscovered; closing it would mean\n   deciding what the suite should say about a role recorded twice, which is a larger question than\n   any of these.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with the two\npre-existing advisories and no new one; the suite's verdict against `rloop-bash` `02c6206` still\n`19 passed, 11 failed` with the same eleven names; every quotation cites its owner.\n","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T09:52:11.654587902Z","created_by":"master","updated_at":"2026-09-25T10:11:36.760912954Z","closed_at":"2026-09-25T10:11:36.760854965Z","close_reason":"CNF-11's clause narrowed to the Round 1 judge call it reads and pointed at CNF-23; CNF-23 names its two-Round shape (commit 62aad4c). Item 3 of the entry was recorded, not a defect, and stays open as a note. Panel nits filed as rl-cnf11-cnf23-wording-nits-8m1; the claude-preset reach gap the narrowing exposed as rl-claude-judge-session-id-reach-a1j.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf11-cnf23-wording-nits-8m1","title":"CNF-11 and CNF-23 wording nits from the rl-cnf11-judge-id-second-home-93a Panel: 'sends the brief back' where the document says 'rewrite'; 'reads them all' without CNF-23's codex scope","description":"Suggestions from the Panel of Run `20260925T095253.818886783Z-2268035`\n(`rl-cnf11-judge-id-second-home-93a`, commit `62aad4c`), raised by the opus Reviewer. Nothing is\nred: the gates exit 0 with the two standing advisories, and the suite's verdict against\n`rloop-bash` `02c6206` is `19 passed, 11 failed` with the eleven names it had before. Both are\nprose, in `06-conformance.md`, in the two clauses that Run wrote.\n\n1. `06-conformance.md` `CNF-23` writes `Round 1 sends the brief back` where this document already\n   has a word for it. The suite's knob is `judge=rewrite` (`conformance/run:265`), `CONTEXT.md`\n   says of the Task File `The Manager rewrites it to start another Round`, and `CNF-30` writes the\n   same shape as `Round 1 judged `rewrite`` (`06-conformance.md:318`). `sends the brief back` also\n   collides with `PRM-2`'s idiom for feedback — `03-prompts.md:59`, `send back only what makes the\n   implementation fail the brief` — so it can be read as the judge returning findings rather than\n   starting a Round. `Round 1 judges `rewrite`` matches the precedent and costs one word. The\n   phrase came from `conformance/run:261`'s own comment, so this is consistency, not a defect.\n\n2. `06-conformance.md` `CNF-11` says `CNF-23` `is the item that reads them all` without naming\n   `CNF-23`'s scope. `CNF-23` opens `With `--manager codex``, so it reads every judge call of a\n   codex Run. Read that way the clause is exact; read as every judge call anywhere it\n   over-promises. Naming the scope — \"reads them all for the Run it asserts\", or similar — closes\n   the second reading. The phrase was the brief's own wording, so the Implementer departed from\n   nothing.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with the two\npre-existing advisories and no new one; the suite's verdict against `rloop-bash` `02c6206` still\n`19 passed, 11 failed` with the same eleven names; every quotation cites its owner.\n","notes":"Item 2 landed in commit 0e74dbc (rl-claude-judge-session-id-reach-a1j): CNF-11's clause now reads 'every judge call of a Run is read by CNF-23 under --manager codex and by CNF-33 under the default preset', which names the scope this item asked for. That Run's opus Reviewer offers a tighter form of the same clause; it is item 2 of rl-cnf33-panel-nits-agj. Item 1 of this issue, CNF-23's 'sends the brief back', stands untouched.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T10:11:18.552165033Z","created_by":"master","updated_at":"2026-09-25T10:41:53.835510814Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-claude-judge-session-id-reach-a1j","title":"No item reads a --manager claude Run's Round 2 judge call for the session id (RUN-16 reach, sibling of rl-csw)","description":"Raised by the opus Reviewer of Run `20260925T095253.818886783Z-2268035`\n(`rl-cnf11-judge-id-second-home-93a`, commit `62aad4c`). Nothing is red; this is reach, and the\nsibling of `rl-csw`, which closed the same gap for the codex preset.\n\n`RUN-16` covers both presets: `The Manager MUST be one session for the whole Run: the pick\nestablishes it, every judge call resumes it (`AGT-3`, `AGT-4`), and rloop MUST write its id to the\n`session` file (`DIR-4`)`. The only place the suite reads a Round 2 or later judge argument list\nfor the session id is `CNF-23`'s loop over `\"$rec\"/*-manager-[1-9]*.argv` (`conformance/run:335`),\nand that Run is a `--manager codex` Run. The other reads of a later judge record check something\nelse: `conformance/run:357` is `CNF-13`, which greps the base hash out of the prompt, and `:759` is\n`CNF-30`, which checks only that no Round 2 judge argument list exists. `CNF-11`'s block reads each\nRun's Round 1 judge record alone.\n\nSo under `--manager claude` an Implementation that resumed Round 2 under a different id than the\n`session` file's would pass the suite. The reach was always this; what commit `62aad4c` changed is\nthat `CNF-11`'s prose no longer covers for it — it now says what it reads, which is the Round 1\ncall.\n\nThe question this opens is which item should own the claude preset's every-judge-call reading.\n`CNF-11` reads argument lists per role and would have to grow a loop; `CNF-23` is the\ncodex-session item by its own first clause and is the wrong home; a third item, or a widened\n`CNF-11` clause plus its loop, are the candidates. Whoever takes it decides that first, then\nwrites the reading, then gives the item a shape that names the Round count as `CNF-23` and\n`CNF-24` now do.\n\nAcceptance: an item of `06-conformance.md` reads every judge call of a `--manager claude` Run of\nmore than one Round and asserts each carries the `session` file's id; `nix develop --command bash\ntools/check-all.sh` exits 0, unpiped, with the two pre-existing advisories and no new one; the\nsuite's verdict against `rloop-bash` `02c6206` is recorded before and after, and any change in it\nis explained.\n","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T10:11:32.311288252Z","created_by":"master","updated_at":"2026-09-25T10:42:00.134973278Z","closed_at":"2026-09-25T10:42:00.134922613Z","close_reason":"Landed in commit 0e74dbc, one Round. The Consultation (opus and astra, both answering, both choosing B; fable out of quota and counted as not answering) settled the item's home: CNF-33, a default-preset sibling of CNF-23, rather than a widened CNF-11. CNF-33 runs a Run of its own — no --manager flag, two Rounds, Round 1 judging rewrite and Round 2 done — reads both judge records for the session file's id, names the Round count and fails on one record or none; CNF-11's pointer now names both preset owners. Gate exit 0 unpiped with the two standing advisories and no new one; suite against rloop-bash 02c6206 moved 19 passed, 11 failed to 20 passed, 11 failed with the same eleven names, CNF-33 green there because that build carries the resume id correctly and is red on CNF-11 over the judge call's other arguments. Seen red on scratch copies for a wrong id, a one-Round Run and an empty glob. The Panel's nits are rl-cnf33-panel-nits-agj and rl-grep-fixed-string-session-id-113.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-cnf33-panel-nits-agj","title":"CNF-33 and CNF-11 nits from the rl-claude-judge-session-id-reach-a1j Panel: a false reason in the non-empty-id guard's comment; the pointer's 'every judge call of a Run'; an orphan line","description":"Suggestions from the Panel of Run `20260925T101213.513558317Z-2268035`\n(`rl-claude-judge-session-id-reach-a1j`, commit `0e74dbc`): item 1 from the astra Reviewer, items\n2 and 3 from the opus Reviewer. Nothing is red — the gates exit 0 with the two standing advisories\nand the suite against `rloop-bash` `02c6206` is `20 passed, 11 failed` with the eleven names it had\nbefore, `CNF-33` green.\n\n1. `conformance/run:836`-`:837`. The comment above `CNF-33`'s non-empty-id guard reads\n\n   > a non-empty id before the comparison: `grep -qx` over an empty one matches the trailing empty\n   > line every NUL-terminated argument list ends with\n\n   and the reason is false. `tr '\\0' '\\n'` turns the record's final NUL into the last line's\n   terminator, not into an empty line: over `claude\\0-p\\0prompt\\0--resume\\0abc\\0`, `grep -qx -- ''`\n   exits 1. An empty line appears only when the argument list holds an actual empty argument\n   (`a\\0\\0b\\0`), where `grep -qx -- ''` exits 0. The Manager confirmed both. The guard is worth\n   keeping — an empty `session` file gets its own detail instead of two `does not carry the id`\n   lines — so replace the reason, do not delete the guard.\n\n2. `06-conformance.md:103`. `CNF-11`'s new pointer reads `every judge call of a Run is read by\n   CNF-23 under --manager codex and by CNF-33 under the default preset`. It is strictly better than\n   the `is the item that reads them all` it replaced, and it closes item 2 of\n   `rl-cnf11-cnf23-wording-nits-8m1`, but it still parses as a claim about any codex Run, where each\n   item reads the one Run it asserts and `CNF-33`'s Run is not one of `CNF-11`'s. The opus Reviewer\n   offers `the item that reads a Run's judge calls all is CNF-23 under --manager codex and CNF-33\n   under the default preset`, which owns the ownership claim alone.\n\n3. `06-conformance.md:104`. The pointer's edit left a 19-character orphan line mid-paragraph.\n   Reflow it. The same item is already ragged at `:108`, so this changes nothing that matters and\n   is worth doing only alongside item 2.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with the two\npre-existing advisories and no new one; no requirement and no suite behaviour changes; the suite's\nverdict against `rloop-bash` `02c6206` is still `20 passed, 11 failed` with the same eleven names.\n","notes":"2026-09-25, from the Panel of Run 20260925T104239.796514290Z-2268035 (rl-grep-fixed-string-session-id-113):\nitem 1's comment is now stale in a second way. The comparison nine lines below it is `grep -Fqx`\nsince that Run, so the comment's `grep -qx over an empty one matches the trailing empty line` names a\nflag the block no longer carries as well as giving a false reason. Both Implementers left it\ndeliberately -- renaming the flag inside a false sentence makes the comment look maintained without\nmaking it true. Whoever takes this item rewrites the reason and the flag in one edit.\n\nThe `-F` change does not affect the guard's need: `printf 'a\\0b\\0' | tr '\\0' '\\n' | grep -qx -- ''`\nand the same with `-Fqx` both exit 1, and `printf 'a\\0\\0b\\0' | tr '\\0' '\\n' | grep -Fqx -- ''` exits\n0, so an empty pattern still matches only an actual empty argument, either way.","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T10:41:37.032545533Z","created_by":"master","updated_at":"2026-09-25T11:37:44.639166042Z","closed_at":"2026-09-25T11:37:44.639115137Z","close_reason":"All three edits landed at f357606: conformance/run's guard comment now states what the guard buys (one detail line for an empty or absent session file) and names grep -Fqx; CNF-11's pointer reads 'the item that reads a Run's judge calls all is CNF-23 under --manager codex and CNF-33 under the default preset'; the paragraph is reflowed with no orphan line. Gates exit 0 with the same two advisories (01-run-lifecycle.md:249 restatement, ADR-0006:11 citation) and no third; the suite against rloop-bash 02c6206 is still 20 passed, 11 failed with the same eleven names (CNF-3 CNF-5 CNF-8 CNF-9 CNF-11 CNF-12 CNF-14 CNF-17 CNF-20 CNF-26 CNF-32), CNF-33 green. The brief's printf claim was verified here: printf 'a\\0b\\0' | tr '\\0' '\\n' | grep -Fqx -- '' exits 1, printf 'a\\0\\0b\\0' the same exits 0.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-grep-fixed-string-session-id-113","title":"CNF-33's and CNF-23's session-id comparison is grep -qx, so the id is a regex, not a string","description":"Raised by the opus Reviewer of Run `20260925T101213.513558317Z-2268035`\n(`rl-claude-judge-session-id-reach-a1j`, commit `0e74dbc`). Nothing is red; this is reach.\n\nBoth session-id readings compare the id with `grep -qx -- \"$sid\"`, which treats the id as a basic\nregular expression rather than as a string: `conformance/run:846` (`CNF-33`) and\n`conformance/run:369` (`CNF-23`).\n\nUnder `--manager codex` the id is the fake's own hardcoded UUID and `CNF-23` compares it against\nthat literal first, so nothing the Implementation chooses reaches the pattern. `CNF-33` is the\nfirst place it does: under the default preset `AGT-3` says the pick MUST be\n`claude -p <pick prompt> --session-id <session id> …` with an id `RUN-16` says is `one rloop chose\nbefore the pick`, and `CNF-33` reads whatever that Implementation wrote to `session`. An\nImplementation whose `session` file held `.*` would pass `CNF-33` vacuously — it would fail\n`CNF-9`, which requires the file to hold `the Manager's session id as a UUID`, so no conforming\nImplementation is at risk and nothing is red today.\n\n`grep -Fqx` is one flag, inside `CNF-1`'s toolbox, and makes the comparison the string equality\nboth items' prose describes. Whoever takes it should decide first whether `CNF-23`'s line moves\ntoo — the two lines are identical and leaving one behind is the kind of split this repository files\ntickets about — and whether either item's prose needs to say the comparison is of bytes, or whether\nthe existing wording already carries it.\n\nAcceptance: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with the two\npre-existing advisories and no new one; the suite against `rloop-bash` `02c6206` still reports\n`20 passed, 11 failed` with the same eleven names; the changed comparison is demonstrated to still\ngo red, on a scratch copy, for a wrong id.\n","status":"closed","priority":4,"issue_type":"task","created_at":"2026-09-25T10:41:47.792780681Z","created_by":"master","updated_at":"2026-09-25T11:31:17.232581642Z","closed_at":"2026-09-25T11:31:17.232531929Z","close_reason":"Done in Run 20260925T104239.796514290Z-2268035. Both session-id comparisons are `grep -Fqx`:\n`conformance/run:338` (CNF-23) and `:845` (CNF-33). No Markdown changed -- `given that same id` and\n`carries the session file's id` are already identities.\n\nVerified by the Manager: gates exit 0, unpiped, with the two standing advisories and no new one;\n`bash conformance/run /home/master/p/rloop-bash/result/bin/rloop` is `20 passed, 11 failed` with the\nsame eleven names, CNF-23 and CNF-33 green. The Implementer and the opus Reviewer each showed, on\nscratch copies, that `sid='.*'` passes CNF-33 vacuously under `grep -qx` and reddens it under\n`grep -Fqx`, and that both changed lines go red for a wrong UUID naming `009-manager-1.argv` and\n`016-manager-2.argv`.\n\nFollow-ups filed: the stale `grep -qx` citation in `conformance/run:836`'s comment is noted on\nrl-cnf33-panel-nits-agj, which already owns that comment's false reason; the three env-line checks\nthat interpolate a mktemp path into a BRE are rl-rmg.","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
{"id":"rl-rmg","title":"The suite's env-line checks interpolate a mktemp path into a BRE; -F only if the file is swept","description":"Raised by the opus Reviewer of Run 20260925T104239.796514290Z-2268035\n(rl-grep-fixed-string-session-id-113). Nothing is red, and the Reviewer's own verdict is that this\nis not a hazard. Filed so it is not rediscovered from scratch.\n\nThat Run made the two session-id comparisons `grep -Fqx` (`conformance/run:338`, `:845`) and fenced\noff every other `grep -qx` in the file. Of those, three interpolate a path built by `mktemp` into a\nbasic regular expression:\n\n- `conformance/run:168`  `grep -qx \"RUN_DIR=$repo/run\" \"$f\"`\n- `conformance/run:170`  `grep -qx \"CWD=$repo\" \"$f\"`\n- `conformance/run:560`  `grep -qx \"CWD=$(cd \"$repo\" && pwd -P)\" \"$ef\"`\n\nThe template is `rloop-conformance.XXXXXX`, so the pattern carries a literal `.` that matches any\ncharacter. Why it is not a hazard: a regex is never stricter than the string it came from, the\nrecorded line holds the same character the `.` stands for, and a false pass would need an\nImplementation to write a `RUN_DIR=`/`CWD=` line differing from the expected path in exactly that one\nbyte position. `pid_in` (`:409`) and the `ROLE=`/`ROUND=`/`RUN=`/`REVIEWER=`/`STDIN=` checks compare\nagainst literals the suite writes and carry no metacharacter at all.\n\nWorth doing only as part of a sweep of the file for other reasons: `-F` on the three lines, and a\ndecision on whether the metacharacter-free siblings move with them for uniformity. `-F` is inside\n`CNF-1`'s toolbox.\n\nAcceptance, if taken: `nix develop --command bash tools/check-all.sh` exits 0, unpiped, with the two\npre-existing advisories and no new one; the suite against `rloop-bash` `02c6206` still reports\n`20 passed, 11 failed` with the same eleven names; no requirement changes.","status":"open","priority":4,"issue_type":"task","created_at":"2026-09-25T11:31:10.512807307Z","created_by":"master","updated_at":"2026-09-25T11:31:10.512807307Z","source_repo":"rloop","source_repo_path":"/home/master/p/rloop","compaction_level":0,"original_size":0}
